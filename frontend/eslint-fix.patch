--- frontend/e2e/keyboard-focus-navigation.spec.ts
+++ frontend/e2e/keyboard-focus-navigation.spec.ts
@@ -66,7 +66,7 @@
     await page.goto('/');

     // First element should receive focus
-    let previousElement = null;
+    let _previousElement = null;

     // Press Tab 5 times to navigate through main navigation
     for (let i = 0; i < 5; i++) {
@@ -139,7 +139,7 @@
     await uploadZone.focus();

     // Check if focus-visible class or outline is applied
-    const isFocusVisible = await uploadZone.evaluate((el) => {
+    const _isFocusVisible = await uploadZone.evaluate((el) => {
       return el.matches(':focus-visible') ||
              window.getComputedStyle(el).outlineStyle !== 'none' ||
              window.getComputedStyle(el).boxShadow !== 'none';
