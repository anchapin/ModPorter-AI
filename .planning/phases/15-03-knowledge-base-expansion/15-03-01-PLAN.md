---
phase: 15-03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/ingestion/pipeline.py
  - backend/src/ingestion/sources/__init__.py
  - backend/src/ingestion/sources/base.py
  - backend/src/ingestion/sources/forge_docs.py
  - backend/src/ingestion/sources/fabric_docs.py
  - backend/src/ingestion/sources/bedrock_docs.py
  - backend/src/ingestion/processors/markdown.py
  - backend/src/ingestion/processors/html.py
  - backend/src/ingestion/validators/quality.py
  - backend/tests/integration/test_kb_ingestion.py
  - backend/tests/fixtures/knowledge_fixtures.py
autonomous: true
requirements:
  - RAG-3.1
user_setup: []

must_haves:
  truths:
    - "Documentation ingestion pipeline can fetch from external sources (Forge, Fabric, Bedrock)"
    - "Ingested documents are chunked using smart strategies from Phase 15-01"
    - "Metadata is extracted and enriched (API version, mod loader, game version)"
    - "Quality validators filter low-quality content before indexing"
    - "Ingestion runs asynchronously without blocking API"
  artifacts:
    - path: "backend/src/ingestion/pipeline.py"
      provides: "Main ingestion orchestrator"
      min_lines: 100
      exports: ["IngestionPipeline", "ingest_source"]
    - path: "backend/src/ingestion/sources/base.py"
      provides: "Base source adapter interface"
      exports: ["BaseSourceAdapter", "RawDocument", "DocumentType"]
    - path: "backend/src/ingestion/sources/forge_docs.py"
      provides: "Forge documentation scraper"
      exports: ["ForgeDocsAdapter"]
    - path: "backend/src/ingestion/sources/fabric_docs.py"
      provides: "Fabric documentation scraper"
      exports: ["FabricDocsAdapter"]
    - path: "backend/src/ingestion/sources/bedrock_docs.py"
      provides: "Bedrock API reference fetcher"
      exports: ["BedrockDocsAdapter"]
    - path: "backend/src/ingestion/processors/markdown.py"
      provides: "Markdown document processor"
      exports: ["MarkdownProcessor"]
    - path: "backend/src/ingestion/processors/html.py"
      provides: "HTML document processor"
      exports: ["HTMLProcessor"]
    - path: "backend/src/ingestion/validators/quality.py"
      provides: "Quality validation checks"
      exports: ["QualityValidator"]
    - path: "backend/tests/integration/test_kb_ingestion.py"
      provides: "Integration tests for ingestion"
      min_lines: 150
  key_links:
    - from: "backend/src/ingestion/pipeline.py"
      to: "ai-engine/indexing/chunking_strategies.py"
      via: "from indexing.chunking_strategies import ChunkingStrategyFactory"
      pattern: "ChunkingStrategyFactory.create"
    - from: "backend/src/ingestion/pipeline.py"
      to: "ai-engine/indexing/metadata_extractor.py"
      via: "from indexing.metadata_extractor import DocumentMetadataExtractor"
      pattern: "DocumentMetadataExtractor"
    - from: "backend/src/ingestion/pipeline.py"
      to: "backend/src/db/crud.py"
      via: "from db import crud"
      pattern: "crud.create_document_with_chunks"
---

<objective>
Implement modular documentation ingestion pipeline for Minecraft modding documentation

Purpose: Enable automatic ingestion and indexing of Minecraft Forge, Fabric, and Bedrock API documentation into the RAG knowledge base, expanding coverage for conversion patterns and API references

Output: Working ingestion pipeline with source adapters for Forge/Fabric/Bedrock docs, quality validation, and async processing
</objective>

<execution_context>
@/home/alex/Projects/ModPorter-AI/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/Projects/ModPorter-AI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@ai-engine/indexing/chunking_strategies.py
@ai-engine/indexing/metadata_extractor.py
@backend/src/api/embeddings.py
@backend/src/db/models.py
@backend/src/db/crud.py
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Create ingestion infrastructure and base source adapter</name>
  <files>
    backend/src/ingestion/__init__.py
    backend/src/ingestion/pipeline.py
    backend/src/ingestion/sources/__init__.py
    backend/src/ingestion/sources/base.py
  </files>
  <read_first>
    - ai-engine/indexing/chunking_strategies.py (for ChunkingStrategyFactory pattern)
    - ai-engine/indexing/metadata_extractor.py (for DocumentMetadataExtractor pattern)
    - backend/src/db/crud.py (for create_document_with_chunks function)
  </read_first>
  <action>
    Create the base infrastructure for documentation ingestion:

    1. **backend/src/ingestion/__init__.py**: Empty init file for package

    2. **backend/src/ingestion/pipeline.py**: Create IngestionPipeline class with:
       - `__init__(db: AsyncSession)`: Initialize with database session
       - `async def ingest_source(source_name: str, config: Dict[str, Any], chunking_strategy: str = "semantic")`: Main ingestion method
       - Private methods: `_load_sources()`, `_load_processors()`
       - Integration with ChunkingStrategyFactory and DocumentMetadataExtractor
       - Deduplication check using content_hash
       - Returns dict: {source, documents_processed, chunks_indexed, status}

    3. **backend/src/ingestion/sources/__init__.py**: Empty init file

    4. **backend/src/ingestion/sources/base.py**: Create base classes:
       - `DocumentType(Enum)`: MARKDOWN, HTML, PDF, CODE
       - `@dataclass RawDocument`: content, source_url, doc_type, metadata, title
       - `BaseSourceAdapter(ABC)` with abstract methods:
         - `async def fetch(self, config: dict) -> List[RawDocument]`
         - `def validate_config(self, config: dict) -> bool`

    Use the same patterns as ChunkingStrategyFactory (factory pattern) and DocumentMetadataExtractor (metadata enrichment).
  </action>
  <verify>
    <automated>python -c "from backend.src.ingestion.pipeline import IngestionPipeline; from backend.src.ingestion.sources.base import BaseSourceAdapter, RawDocument, DocumentType; print('Imports successful')"</automated>
  </verify>
  <done>
    - IngestionPipeline class can be imported
    - BaseSourceAdapter interface defined with fetch() and validate_config()
    - RawDocument dataclass has all required fields
    - DocumentType enum has MARKDOWN, HTML, PDF, CODE values
  </done>
</task>

<task type="auto">
  <name>Task 1.2: Implement documentation source adapters (Forge, Fabric, Bedrock)</name>
  <files>
    backend/src/ingestion/sources/forge_docs.py
    backend/src/ingestion/sources/fabric_docs.py
    backend/src/ingestion/sources/bedrock_docs.py
  </files>
  <read_first>
    - backend/src/ingestion/sources/base.py (for BaseSourceAdapter interface)
  </read_first>
  <action>
    Create three source adapters extending BaseSourceAdapter:

    1. **backend/src/ingestion/sources/forge_docs.py**: ForgeDocsAdapter
       - BASE_URL = "https://docs.minecraftforge.net"
       - `async def fetch()`: Fetch documentation using aiohttp
       - Handle both markdown and HTML pages
       - Extract metadata: mod_loader="forge", version from config
       - Returns List[RawDocument] with DocumentType.MARKDOWN or HTML

    2. **backend/src/ingestion/sources/fabric_docs.py**: FabricDocsAdapter
       - BASE_URL = "https://fabricmc.net/wiki"
       - Similar structure to ForgeDocsAdapter
       - Extract metadata: mod_loader="fabric", version from config
       - Returns List[RawDocument]

    3. **backend/src/ingestion/sources/bedrock_docs.py**: BedrockDocsAdapter
       - BASE_URL = "https://learn.microsoft.com/en-us/minecraft/creator/"
       - API_REFERENCE_URL = BASE_URL + "script/scriptapi/"
       - `async def fetch()`: Fetch Bedrock Script API reference pages
       - Accept config: {namespaces: ["minecraft", "mojang-minecraft", "mojang-gametest"], game_version: "1.21.0"}
       - Extract metadata: api_type="script_api", namespace, game_version
       - Returns List[RawDocument] with DocumentType.HTML

    Each adapter should:
    - Use aiohttp for async HTTP requests
    - Set 30-second timeout
    - Handle errors gracefully (log but continue)
    - Validate config in validate_config()
  </action>
  <verify>
    <automated>pytest backend/tests/integration/test_kb_ingestion.py::test_source_adapters -x 2>/dev/null || echo "Test file not created yet, verifying imports manually"</automated>
    <automated>python -c "from backend.src.ingestion.sources.forge_docs import ForgeDocsAdapter; from backend.src.ingestion.sources.fabric_docs import FabricDocsAdapter; from backend.src.ingestion.sources.bedrock_docs import BedrockDocsAdapter; print('All adapters import successfully')"</automated>
  </verify>
  <done>
    - ForgeDocsAdapter can be instantiated and has fetch() method
    - FabricDocsAdapter can be instantiated and has fetch() method
    - BedrockDocsAdapter can be instantiated and has fetch() method
    - All adapters extend BaseSourceAdapter
    - validate_config() works for all adapters
  </done>
</task>

<task type="auto">
  <name>Task 1.3: Implement document processors and quality validators</name>
  <files>
    backend/src/ingestion/processors/__init__.py
    backend/src/ingestion/processors/markdown.py
    backend/src/ingestion/processors/html.py
    backend/src/ingestion/validators/__init__.py
    backend/src/ingestion/validators/quality.py
  </files>
  <read_first>
    - backend/src/ingestion/sources/base.py (for RawDocument structure)
    - ai-engine/indexing/metadata_extractor.py (for metadata extraction patterns)
  </read_first>
  <action>
    Create document processors and quality validators:

    1. **backend/src/ingestion/processors/__init__.py**: Empty init

    2. **backend/src/ingestion/processors/markdown.py**: MarkdownProcessor
       - Use `markdown` library with extensions: fenced_code, tables, toc, meta
       - `def process(doc: RawDocument) -> Dict[str, Any]`:
         - Convert markdown to HTML
         - Extract metadata: title (from first heading), toc, code_blocks, word_count
         - Merge with existing metadata from doc.metadata
       - `_extract_title(content: str, default: str) -> str`: Extract first heading
       - `_extract_code_blocks(content: str) -> list`: Extract code blocks with language info using regex

    3. **backend/src/ingestion/processors/html.py**: HTMLProcessor
       - Use BeautifulSoup4 for parsing
       - `def process(doc: RawDocument) -> Dict[str, Any]`:
         - Extract main content (remove nav, footer, sidebar)
         - Extract title from <title> or <h1>
         - Extract code blocks (<pre><code>)
         - Extract links and headings
         - Return processed dict with content, html_content, metadata

    4. **backend/src/ingestion/validators/__init__.py**: Empty init

    5. **backend/src/ingestion/validators/quality.py**: QualityValidator
       - `MIN_LENGTH = 50` (minimum characters)
       - `MAX_LENGTH = 100000` (maximum characters)
       - `def validate(content: str, metadata: dict) -> ValidationResult`:
         - Check length bounds
         - Check for meaningful content (not just whitespace/special chars)
         - Check metadata completeness (title, source)
         - Returns ValidationResult with is_valid, errors, warnings
       - `def _is_meaningful(content: str) -> bool`: Check alphanumeric content ratio > 0.3

    Processors should extract metadata that complements DocumentMetadataExtractor from Phase 15-01.
  </action>
  <verify>
    <automated>python -c "from backend.src.ingestion.processors.markdown import MarkdownProcessor; from backend.src.ingestion.processors.html import HTMLProcessor; from backend.src.ingestion.validators.quality import QualityValidator; print('All processors and validators import successfully')"</automated>
    <automated>pytest backend/tests/integration/test_kb_ingestion.py::test_processors -x 2>/dev/null || echo "Test file not created yet"</automated>
  </verify>
  <done>
    - MarkdownProcessor.process() returns dict with title, toc, code_blocks, word_count
    - HTMLProcessor.process() extracts main content, title, code blocks
    - QualityValidator.validate() rejects content < 50 chars or > 100000 chars
    - QualityValidator checks for meaningful content (alphanumeric ratio > 0.3)
    - All processors can handle RawDocument input
  </done>
</task>

<task type="auto">
  <name>Task 1.4: Create integration tests for ingestion pipeline</name>
  <files>
    backend/tests/fixtures/knowledge_fixtures.py
    backend/tests/integration/test_kb_ingestion.py
  </files>
  <read_first>
    - backend/tests/integration/test_document_indexing.py (for test patterns from Phase 15-01)
    - backend/tests/conftest.py (for existing test fixtures)
  </read_first>
  <action>
    Create integration tests for the ingestion pipeline:

    1. **backend/tests/fixtures/knowledge_fixtures.py**: Test fixtures
       - `@pytest.fixture async def mock_ingestion_db()`: Async test database
       - `@pytest.fixture def sample_markdown_doc()`: Sample RawDocument with markdown
       - `@pytest.fixture def sample_html_doc()`: Sample RawDocument with HTML
       - `@pytest.fixture def mock_aiohttp_response()`: Mock HTTP responses

    2. **backend/tests/integration/test_kb_ingestion.py**: Integration tests
       - `async def test_source_adapters()`: Test all three adapters can be instantiated
       - `async def test_markdown_processor()`: Test markdown processing and metadata extraction
       - `async def test_html_processor()`: Test HTML processing and content extraction
       - `async def test_quality_validator()`: Test quality checks (min/max length, meaningful content)
       - `async def test_ingest_forge_docs()`: End-to-end test of Forge docs ingestion (mock HTTP)
       - `async def test_ingest_bedrock_api()`: End-to-end test of Bedrock API ingestion (mock HTTP)
       - `async def test_deduplication()`: Test that duplicate content is not re-indexed (content_hash check)
       - Use pytest-asyncio for async tests
       - Mock aiohttp responses to avoid external HTTP calls in tests
       - Verify documents are created in database with correct metadata
       - Verify chunks are created using ChunkingStrategyFactory

    Follow the same patterns as test_document_indexing.py from Phase 15-01.
  </action>
  <verify>
    <automated>pytest backend/tests/integration/test_kb_ingestion.py -v --tb=short 2>&1 | head -100</automated>
  </verify>
  <done>
    - All 8 integration tests pass
    - test_source_adapters verifies all adapters can be imported
    - test_markdown_processor verifies title, toc, code_blocks extraction
    - test_html_processor verifies content extraction and metadata
    - test_quality_validator rejects short/long content
    - test_ingest_forge_docs creates documents and chunks in database
    - test_ingest_bedrock_api creates documents with correct metadata
    - test_deduplication prevents duplicate content_hash from being inserted
  </done>
</task>

</tasks>

<verification>
## Phase Verification Criteria

- [ ] Documentation ingestion pipeline can fetch from Forge, Fabric, and Bedrock sources
- [ ] Ingested documents are processed (markdown/HTML) and quality-validated
- [ ] Documents are chunked using ChunkingStrategyFactory from Phase 15-01
- [ ] Metadata is extracted and enriched (mod_loader, api_version, game_version)
- [ ] Deduplication prevents re-indexing same content (content_hash check)
- [ ] All integration tests pass (8 tests)
- [ ] Ingestion runs asynchronously (async/await pattern used throughout)

## Manual Verification

Test the ingestion with real documentation sources (optional, can be done in Phase 15-04):
```bash
# Ingest Forge documentation
curl -X POST http://localhost:8080/api/v1/knowledge-base/ingest \
  -H "Content-Type: application/json" \
  -d '{
    "source": "forge_docs",
    "config": {
      "version": "1.20.1",
      "sections": ["getting-started", "blocks"]
    },
    "chunking_strategy": "semantic"
  }'

# Ingest Bedrock API reference
curl -X POST http://localhost:8080/api/v1/knowledge-base/ingest \
  -H "Content-Type: application/json" \
  -d '{
    "source": "bedrock_docs",
    "config": {
      "namespaces": ["minecraft", "mojang-minecraft"],
      "game_version": "1.21.0"
    }
  }'
```
</verification>

<success_criteria>
- Ingestion pipeline can fetch and process documentation from 3 sources (Forge, Fabric, Bedrock)
- Quality validation filters low-quality content before indexing
- Documents are indexed with hierarchical structure (parent + chunks) from Phase 15-01
- Integration tests verify end-to-end flow works correctly
- No duplicate content (content_hash deduplication working)
- Metadata enrichment provides context for search (mod_loader, API version, game version)
</success_criteria>

<output>
After completion, create `.planning/phases/15-03-knowledge-base-expansion/15-03-01-SUMMARY.md`
</output>
