---
phase: 15-03
plan: 02
type: execute
wave: 2
depends_on: ["15-03-01"]
files_modified:
  - ai-engine/knowledge/patterns/__init__.py
  - ai-engine/knowledge/patterns/base.py
  - ai-engine/knowledge/patterns/java_patterns.py
  - ai-engine/knowledge/patterns/bedrock_patterns.py
  - ai-engine/knowledge/patterns/mappings.py
  - ai-engine/knowledge/community/__init__.py
  - ai-engine/knowledge/community/submission.py
  - ai-engine/knowledge/community/review.py
  - ai-engine/knowledge/community/validation.py
  - backend/src/api/knowledge_base.py
  - backend/src/db/models.py (add PatternSubmission model)
  - backend/tests/integration/test_community_workflow.py
autonomous: true
requirements:
  - RAG-3.2
user_setup: []

must_haves:
  truths:
    - "Conversion pattern library stores Java→Bedrock pattern mappings"
    - "Community members can submit new patterns via API"
    - "Submitted patterns are validated (syntax, quality, malicious content check)"
    - "Reviewers can approve/reject pattern submissions"
    - "Approved patterns are added to the pattern library for RAG retrieval"
    - "Pattern metadata includes contributor, upvotes/downvotes, tags"
  artifacts:
    - path: "ai-engine/knowledge/patterns/base.py"
      provides: "Base pattern class and interfaces"
      exports: ["ConversionPattern", "PatternLibrary"]
    - path: "ai-engine/knowledge/patterns/java_patterns.py"
      provides: "Java idiom pattern definitions"
      exports: ["JavaPatternRegistry"]
    - path: "ai-engine/knowledge/patterns/bedrock_patterns.py"
      provides: "Bedrock equivalent pattern definitions"
      exports: ["BedrockPatternRegistry"]
    - path: "ai-engine/knowledge/patterns/mappings.py"
      provides: "Java→Bedrock pattern mappings"
      exports: ["PatternMapping"]
    - path: "ai-engine/knowledge/community/submission.py"
      provides: "Pattern submission handler"
      exports: ["CommunityPatternManager", "PatternSubmission"]
    - path: "ai-engine/knowledge/community/validation.py"
      provides: "Pattern validation logic"
      exports: ["PatternValidator", "ValidationResult"]
    - path: "backend/src/api/knowledge_base.py"
      provides: "Knowledge base API endpoints"
      exports: ["POST /api/v1/knowledge-base/patterns/submit", "POST /api/v1/knowledge-base/patterns/{id}/review"]
    - path: "backend/tests/integration/test_community_workflow.py"
      provides: "Community workflow integration tests"
      min_lines: 200
  key_links:
    - from: "ai-engine/knowledge/community/submission.py"
      to: "backend/src/db/models.py"
      via: "from db.models import PatternSubmission"
      pattern: "PatternSubmission"
    - from: "backend/src/api/knowledge_base.py"
      to: "ai-engine/knowledge/community/submission.py"
      via: "from knowledge.community.submission import CommunityPatternManager"
      pattern: "CommunityPatternManager"
    - from: "ai-engine/knowledge/patterns/mappings.py"
      to: "backend/src/api/embeddings.py"
      via: "RAG retrieval uses pattern library for context"
      pattern: "PatternLibrary.search"
---

<objective>
Build conversion pattern library and community contribution workflow

Purpose: Enable community-driven expansion of the knowledge base through pattern submissions (Java→Bedrock conversion examples), validation, and review workflow, accelerating knowledge base growth beyond official documentation

Output: Working pattern library with 50+ seed patterns, community submission API, validation pipeline, and review workflow
</objective>

<execution_context>
@/home/alex/Projects/portkit/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/Projects/portkit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@ai-engine/indexing/metadata_extractor.py
@backend/src/db/models.py
@backend/src/api/embeddings.py
@.planning/phases/15-03-knowledge-base-expansion/15-03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Create pattern library base classes and registries</name>
  <files>
    ai-engine/knowledge/__init__.py
    ai-engine/knowledge/patterns/__init__.py
    ai-engine/knowledge/patterns/base.py
    ai-engine/knowledge/patterns/java_patterns.py
    ai-engine/knowledge/patterns/bedrock_patterns.py
    ai-engine/knowledge/patterns/mappings.py
  </files>
  <read_first>
    - backend/src/db/models.py (for understanding database model patterns)
    - ai-engine/indexing/metadata_extractor.py (for metadata enrichment patterns)
  </read_first>
  <action>
    Create the pattern library infrastructure:

    1. **ai-engine/knowledge/__init__.py**: Empty init file

    2. **ai-engine/knowledge/patterns/__init__.py**: Export PatternLibrary, JavaPatternRegistry, BedrockPatternRegistry

    3. **ai-engine/knowledge/patterns/base.py**: Base pattern classes
       - `@dataclass ConversionPattern`:
         - id: str
         - name: str
         - description: str
         - java_example: str
         - bedrock_example: str
         - category: str (e.g., "item", "block", "entity", "recipe")
         - tags: List[str]
         - complexity: str ("simple", "medium", "complex")
         - success_rate: float (0.0-1.0, based on actual conversions)
       - `class PatternLibrary`:
         - `patterns: Dict[str, ConversionPattern]`
         - `def add_pattern(pattern: ConversionPattern)`: Add to library
         - `def get_pattern(pattern_id: str) -> Optional[ConversionPattern]`
         - `def search(query: str, category: str = None, limit: int = 10) -> List[ConversionPattern]`
         - `def get_by_category(category: str) -> List[ConversionPattern]`
         - `def get_stats() -> Dict[str, int]`: Total patterns, by category, etc.

    4. **ai-engine/knowledge/patterns/java_patterns.py**: Java pattern registry
       - `class JavaPatternRegistry`:
         - Pre-populate with 25+ common Java patterns:
           - Simple item registration (Item class)
           - Block class with properties
           - Entity type definition
           - Recipe (ShapedRecipe, ShapelessRecipe)
           - Event handlers (PlayerInteractEvent, BlockBreakEvent)
           - Custom capabilities (IItemHandler, IFluidHandler)
           - Tile entity (TileEntity)
           - Network packets (simple ones)
         - Each pattern: id, name, description, java_example, category, tags
       - Use dictionary for O(1) lookup

    5. **ai-engine/knowledge/patterns/bedrock_patterns.py**: Bedrock pattern registry
       - `class BedrockPatternRegistry`:
         - Pre-populate with 25+ Bedrock equivalents:
           - Item definition (minecraft:item)
           - Block definition (minecraft:block)
           - Entity components (minecraft:entity)
           - Recipe formats (minecraft:recipe_shaped, shapeless)
           - Script API event handlers (minecraft:interact, break)
           - Components (minecraft:display_name, durability, stackable)
         - Each pattern: id, name, description, bedrock_example, category, tags
       - Mirror Java registry structure

    6. **ai-engine/knowledge/patterns/mappings.py**: Pattern mappings
       - `@dataclass PatternMapping`:
         - java_pattern_id: str
         - bedrock_pattern_id: str
         - confidence: float (0.0-1.0)
         - notes: str (conversion gotchas, limitations)
       - `class PatternMappingRegistry`:
         - `mappings: Dict[str, PatternMapping]`  # key: java_pattern_id
         - `def add_mapping(mapping: PatternMapping)`
         - `def get_bedrock_equivalent(java_pattern_id: str) -> Optional[PatternMapping]`
         - Pre-populate with 20+ mappings for common patterns

    Pattern examples should be realistic, not "hello world" - use actual Minecraft modding code patterns.
  </action>
  <verify>
    <automated>python -c "from ai-engine.knowledge.patterns import PatternLibrary, JavaPatternRegistry, BedrockPatternRegistry; from ai-engine.knowledge.patterns.mappings import PatternMappingRegistry; lib = PatternLibrary(); print(f'Pattern library loaded with {lib.get_stats()[\"total\"]} patterns')"</automated>
  </verify>
  <done>
    - PatternLibrary can be instantiated and has 50+ patterns (25 Java + 25 Bedrock)
    - JavaPatternRegistry has patterns for items, blocks, entities, recipes, events
    - BedrockPatternRegistry has corresponding Bedrock definitions
    - PatternMappingRegistry has 20+ mappings between Java and Bedrock patterns
    - PatternLibrary.search() returns relevant patterns by query/category
  </done>
</task>

<task type="auto">
  <name>Task 2.2: Implement community pattern submission and validation</name>
  <files>
    ai-engine/knowledge/community/__init__.py
    ai-engine/knowledge/community/submission.py
    ai-engine/knowledge/community/validation.py
  </files>
  <read_first>
    - ai-engine/knowledge/patterns/base.py (for ConversionPattern structure)
    - backend/src/db/models.py (for database model patterns)
  </read_first>
  <action>
    Create community submission and validation system:

    1. **ai-engine/knowledge/community/__init__.py**: Export CommunityPatternManager, PatternValidator

    2. **ai-engine/knowledge/community/submission.py**: Submission handler
       - `class SubmissionStatus(Enum)`: PENDING, UNDER_REVIEW, APPROVED, REJECTED
       - `@dataclass PatternSubmission`:
         - id: str
         - java_pattern: str
         - bedrock_pattern: str
         - description: str
         - contributor_id: str
         - status: SubmissionStatus
         - created_at: datetime
         - reviewed_by: Optional[str]
         - review_notes: Optional[str]
         - upvotes: int
         - downvotes: int
         - tags: List[str]
         - category: str
       - `class CommunityPatternManager`:
         - `async def submit_pattern(self, java_pattern: str, bedrock_pattern: str, description: str, contributor_id: str, tags: List[str], category: str) -> PatternSubmission`
           - Validate pattern using PatternValidator
           - Create submission with PENDING status
           - Store in database (using backend crud)
           - Notify reviewers (log for now)
           - Return submission
         - `async def review_pattern(self, submission_id: str, reviewer_id: str, approved: bool, notes: Optional[str] = None) -> None`
           - Get submission by ID
           - Update status to APPROVED or REJECTED
           - Set reviewed_by and review_notes
           - If approved: add to PatternLibrary
           - Notify contributor (log for now)
         - `async def vote_on_pattern(self, submission_id: str, user_id: str, upvote: bool) -> None`: Handle upvotes/downvotes
         - `async def get_pending_submissions(self, limit: int = 50) -> List[PatternSubmission]`: For reviewers
         - `async def get_submission(self, submission_id: str) -> Optional[PatternSubmission]`

    3. **ai-engine/knowledge/community/validation.py**: Pattern validator
       - `@dataclass ValidationResult`: is_valid: bool, errors: List[str], warnings: List[str]
       - `class PatternValidator`:
         - `MIN_JAVA_LINES = 3`
         - `MIN_BEDROCK_LINES = 3`
         - `MIN_DESCRIPTION_LENGTH = 20`
         - `async def validate_pattern(self, java_pattern: str, bedrock_pattern: str, description: str) -> ValidationResult`:
           - Validate Java pattern (syntax checks, class/interface detection)
           - Validate Bedrock pattern (JSON validation or JavaScript syntax)
           - Validate description length
           - Check for malicious content (eval, exec, __import__, script tags, etc.)
           - Return ValidationResult with errors/warnings
         - `def _validate_java_pattern(self, pattern: str) -> ValidationResult`:
           - Check minimum line count
           - Check for class/interface/enum keywords
           - Check for access modifiers (public/private/protected)
         - `def _validate_bedrock_pattern(self, pattern: str) -> ValidationResult`:
           - If JSON: parse with json.loads()
           - If JavaScript: check for function/const/let/var keywords
           - Check minimum line count
         - `def _contains_malicious_content(self, *patterns: str) -> bool`:
           - Regex check for: eval, __import__, exec, <script, javascript:, document.cookie, Runtime.getRuntime

    Validation should catch common errors and security issues before submission reaches reviewers.
  </action>
  <verify>
    <automated>python -c "from ai-engine.knowledge.community import CommunityPatternManager, PatternValidator; validator = PatternValidator(); result = validator.validate_pattern('public class Test {}', '{\"format_version\": \"1.16.0\"}', 'A simple test pattern'); print(f'Validation result: valid={result.is_valid}, errors={len(result.errors)}')"</automated>
  </verify>
  <done>
    - CommunityPatternManager can be instantiated
    - PatternValidator validates Java patterns (min 3 lines, class keyword)
    - PatternValidator validates Bedrock patterns (JSON or JavaScript)
    - PatternValidator detects malicious content (eval, exec, etc.)
    - submit_pattern() creates PENDING submission with validation
    - review_pattern() approves/rejects and adds to library if approved
  </done>
</task>

<task type="auto">
  <name>Task 2.3: Create database model and API endpoints for community patterns</name>
  <files>
    backend/src/db/models.py (add PatternSubmission model)
    backend/src/db/crud.py (add pattern submission crud)
    backend/src/api/knowledge_base.py
  </files>
  <read_first>
    - backend/src/db/models.py (for existing model patterns, add PatternSubmission)
    - backend/src/db/crud.py (for existing crud patterns, add pattern submission functions)
    - backend/src/api/embeddings.py (for API endpoint patterns)
    - ai-engine/knowledge/community/submission.py (for CommunityPatternManager)
  </read_first>
  <action>
    Add database model and API endpoints for community patterns:

    1. **backend/src/db/models.py**: Add PatternSubmission model
       - `class PatternSubmission(Base)`:
         - __tablename__ = "pattern_submissions"
         - id: Mapped[UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
         - java_pattern: Mapped[str] = mapped_column(Text, nullable=False)
         - bedrock_pattern: Mapped[str] = mapped_column(Text, nullable=False)
         - description: Mapped[str] = mapped_column(Text, nullable=False)
         - contributor_id: Mapped[str] = mapped_column(String, nullable=False, index=True)
         - status: Mapped[str] = mapped_column(String(20), nullable=False, server_default=text("'pending'"))
         - created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
         - reviewed_by: Mapped[Optional[str]] = mapped_column(String, nullable=True)
         - review_notes: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
         - reviewed_at: Mapped[Optional[datetime]] = mapped_column(DateTime(timezone=True), nullable=True)
         - upvotes: Mapped[int] = mapped_column(Integer, nullable=False, server_default=text("0"))
         - downvotes: Mapped[int] = mapped_column(Integer, nullable=False, server_default=text("0"))
         - tags: Mapped[dict] = mapped_column(JSONType, nullable=False, default=list)
         - category: Mapped[str] = mapped_column(String(50), nullable=False, index=True)

    2. **backend/src/db/crud.py**: Add CRUD functions
       - `async def create_pattern_submission(db: AsyncSession, java_pattern: str, bedrock_pattern: str, description: str, contributor_id: str, tags: List[str], category: str) -> PatternSubmission`
       - `async def get_pattern_submission(db: AsyncSession, submission_id: UUID) -> Optional[PatternSubmission]`
       - `async def get_pending_submissions(db: AsyncSession, limit: int = 50) -> List[PatternSubmission]`
       - `async def update_pattern_submission_status(db: AsyncSession, submission_id: UUID, status: str, reviewed_by: str, notes: Optional[str] = None) -> PatternSubmission`
       - `async def vote_on_pattern(db: AsyncSession, submission_id: UUID, upvote: bool) -> PatternSubmission`: Increment upvotes or downvotes
       - Use same async patterns as existing CRUD functions

    3. **backend/src/api/knowledge_base.py**: Create knowledge base API router
       - `router = APIRouter(prefix="/knowledge-base", tags=["knowledge-base"])`
       - `@router.post("/patterns/submit", response_model=PatternSubmissionResponse)`:
         - Accept java_pattern, bedrock_pattern, description, contributor_id, tags, category
         - Use CommunityPatternManager.submit_pattern()
         - Return PatternSubmissionResponse with submission_id, status, created_at
       - `@router.get("/patterns/pending", response_model=List[PatternSubmissionResponse])`:
         - Return pending submissions (for reviewers)
         - Query by status="pending", order by created_at DESC
       - `@router.post("/patterns/{submission_id}/review", response_model=PatternSubmissionResponse)`:
         - Accept approved: bool, notes: Optional[str]
         - Use CommunityPatternManager.review_pattern()
         - Return updated submission
       - `@router.post("/patterns/{submission_id}/vote", response_model=PatternSubmissionResponse)`:
         - Accept upvote: bool
         - Use CommunityPatternManager.vote_on_pattern()
         - Return updated submission with vote counts
       - `@router.get("/patterns/library", response_model=List[ConversionPatternResponse])`:
         - Return all approved patterns from PatternLibrary
         - Support query params: category, tags, limit
       - Define Pydantic models: PatternSubmitRequest, PatternSubmissionResponse, ConversionPatternResponse

    Follow the same async/await patterns as embeddings.py for consistency.
  </action>
  <verify>
    <automated>grep -n "class PatternSubmission" backend/src/db/models.py && grep -n "async def create_pattern_submission" backend/src/db/crud.py && grep -n "router = APIRouter" backend/src/api/knowledge_base.py</automated>
  </verify>
  <done>
    - PatternSubmission model exists in models.py with all required fields
    - CRUD functions exist for create, get pending, update status, vote
    - API router has 5 endpoints: submit, pending, review, vote, library
    - All endpoints use async/await pattern
    - Pydantic models defined for request/response validation
  </done>
</task>

<task type="auto">
  <name>Task 2.4: Create integration tests for community workflow</name>
  <files>
    backend/tests/integration/test_community_workflow.py
  </files>
  <read_first>
    - backend/tests/integration/test_document_indexing.py (for test patterns)
    - backend/tests/fixtures/knowledge_fixtures.py (for shared fixtures)
    - ai-engine/knowledge/community/submission.py (for submission logic)
  </read_first>
  <action>
    Create integration tests for community pattern workflow:

    **backend/tests/integration/test_community_workflow.py**:

    1. `async def test_submit_pattern()`:
       - Submit valid Java→Bedrock pattern
       - Verify submission created with PENDING status
       - Verify validation passes
       - Check database for submission record

    2. `async def test_submit_invalid_pattern()`:
       - Submit pattern with invalid Java (no class keyword)
       - Verify validation fails with error
       - Verify submission NOT created

    3. `async def test_submit_malicious_pattern()`:
       - Submit pattern containing "eval(" or malicious code
       - Verify validation rejects with security error
       - Verify submission NOT created

    4. `async def test_review_pattern_approve()`:
       - Create pending submission
       - Review with approved=True
       - Verify status changes to APPROVED
       - Verify pattern added to PatternLibrary
       - Verify reviewed_by and review_notes set

    5. `async def test_review_pattern_reject()`:
       - Create pending submission
       - Review with approved=False
       - Verify status changes to REJECTED
       - Verify pattern NOT added to PatternLibrary

    6. `async def test_vote_on_pattern()`:
       - Create submission
       - Submit upvote
       - Verify upvotes incremented
       - Submit downvote
       - Verify downvotes incremented

    7. `async def test_get_pending_submissions()`:
       - Create 5 pending submissions
       - Create 2 approved submissions
       - Query pending endpoint
       - Verify returns only pending submissions (5)
       - Verify ordered by created_at DESC

    8. `async def test_pattern_library_search()`:
       - Pre-populate PatternLibrary with patterns
       - Search by query "item"
       - Verify returns item-related patterns
       - Filter by category "block"
       - Verify returns only block patterns

    Use pytest-asyncio, mock database sessions, and follow test patterns from test_document_indexing.py.
  </action>
  <verify>
    <automated>pytest backend/tests/integration/test_community_workflow.py -v --tb=short 2>&1 | head -100</automated>
  </verify>
  <done>
    - All 8 integration tests pass
    - test_submit_pattern creates valid submission
    - test_submit_invalid_pattern rejects invalid Java
    - test_submit_malicious_pattern catches security issues
    - test_review_pattern_approve adds to library
    - test_review_pattern_reject keeps out of library
    - test_vote_on_pattern increments vote counts
    - test_get_pending_submissions filters correctly
    - test_pattern_library_search returns relevant patterns
  </done>
</task>

</tasks>

<verification>
## Phase Verification Criteria

- [ ] Pattern library contains 50+ seed patterns (25 Java + 25 Bedrock)
- [ ] Community members can submit patterns via POST /api/v1/knowledge-base/patterns/submit
- [ ] Pattern validation rejects invalid Java/Bedrock code and malicious content
- [ ] Reviewers can approve/reject patterns via POST /api/v1/knowledge-base/patterns/{id}/review
- [ ] Approved patterns are added to PatternLibrary for RAG retrieval
- [ ] Voting system tracks upvotes/downvotes
- [ ] All integration tests pass (8 tests)

## Manual Verification

Test the community workflow end-to-end:

```bash
# Submit a pattern
curl -X POST http://localhost:8080/api/v1/knowledge-base/patterns/submit \
  -H "Content-Type: application/json" \
  -d '{
    "java_pattern": "public class CustomItem extends Item { public CustomItem() { super(new Item.Properties()); } }",
    "bedrock_pattern": "{\"format_version\": \"1.16.0\", \"minecraft:item\": { \"description\": { \"identifier\": \"test:custom_item\" } } }",
    "description": "Simple custom item registration pattern",
    "contributor_id": "user123",
    "tags": ["item", "simple"],
    "category": "item"
  }'

# Get pending submissions (for reviewer)
curl http://localhost:8080/api/v1/knowledge-base/patterns/pending

# Approve a pattern
curl -X POST http://localhost:8080/api/v1/knowledge-base/patterns/{submission_id}/review \
  -H "Content-Type: application/json" \
  -d '{
    "approved": true,
    "notes": "Good pattern, approved"
  }'

# Search pattern library
curl "http://localhost:8080/api/v1/knowledge-base/patterns/library?category=item&limit=10"
```
</verification>

<success_criteria>
- Pattern library has 50+ seed patterns covering common Java→Bedrock conversions
- Community submission API is functional with validation
- Review workflow approves/rejects patterns and updates library
- Voting system tracks community feedback
- Integration tests verify end-to-end workflow
- Patterns are searchable by category, tags, and text query
</success_criteria>

<output>
After completion, create `.planning/phases/15-03-knowledge-base-expansion/15-03-02-SUMMARY.md`
</output>
