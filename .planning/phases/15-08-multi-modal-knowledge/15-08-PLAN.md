---
phase: 15-08-multi-modal-knowledge
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-engine/utils/texture_metadata_extractor.py
  - ai-engine/utils/model_metadata_extractor.py
  - ai-engine/search/multimodal_search_engine.py
  - ai-engine/search/cross_modal_retriever.py
  - ai-engine/knowledge/__init__.py
  - backend/src/api/knowledge_base.py
  - ai-engine/tests/test_multimodal_search.py
autonomous: true
requirements:
  - RAG-8.1
  - RAG-8.2

must_haves:
  truths:
    - "User can search for textures and get relevant results"
    - "User can search for 3D model documentation and get relevant results"
    - "System can find related content across different modalities (code ↔ texture)"
    - "Texture metadata (dimensions, format, transparency) is indexed and searchable"
  artifacts:
    - path: "ai-engine/utils/texture_metadata_extractor.py"
      provides: "Texture metadata extraction from PNG/JPG files"
      exports: ["TextureMetadataExtractor", "extract_texture_metadata"]
    - path: "ai-engine/utils/model_metadata_extractor.py"
      provides: "3D model documentation processing"
      exports: ["ModelMetadataExtractor", "extract_model_metadata"]
    - path: "ai-engine/search/multimodal_search_engine.py"
      provides: "Multi-modal similarity search"
      exports: ["MultiModalSearchEngine", "search"]
    - path: "ai-engine/search/cross_modal_retriever.py"
      provides: "Cross-modal retrieval"
      exports: ["CrossModalRetriever", "find_related_across_modalities"]
  key_links:
    - from: "texture_metadata_extractor.py"
      to: "multimodal_schema.py"
      via: "ImageMetadata model"
    - from: "model_metadata_extractor.py"
      to: "multimodal_schema.py"
      via: "MultiModalDocument model"
    - from: "multimodal_search_engine.py"
      to: "hybrid_search_engine.py"
      via: "extends HybridSearchEngine"
---

<objective>
Implement multi-modal knowledge support for texture metadata, 3D model documentation, multi-modal similarity search, and cross-modal retrieval.

Purpose: Enable the RAG system to handle texture files and 3D model documentation, allowing users to search across all content types (text, code, texture, model) and find related content across modalities.

Output: New modules for texture/model metadata extraction, multi-modal search, and cross-modal retrieval with unit tests.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-07-advanced-rag-pipeline/15-07-03-SUMMARY.md
@ai-engine/schemas/multimodal_schema.py
@ai-engine/utils/multimodal_embedding_generator.py
@ai-engine/search/hybrid_search_engine.py

## Existing Components (from prior phases)
- multimodal_schema.py: ImageMetadata, MultiModalDocument, EmbeddingVector models
- multimodal_embedding_generator.py: ImageEmbeddingGenerator (mock), MultiModalEmbeddingGenerator
- hybrid_search_engine.py: Has texture-related keywords in query_expansion

## What's Missing
1. Real texture metadata extraction (dimensions, format, transparency, color palette)
2. 3D model documentation processing (Bedrock .json geometry files)
3. Multi-modal similarity search across content types
4. Cross-modal retrieval (finding related content across modalities)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Texture Metadata Extractor</name>
  <files>ai-engine/utils/texture_metadata_extractor.py</files>
  <action>
    Create texture_metadata_extractor.py with TextureMetadataExtractor class that:
    - Extracts dimensions (width, height) from PNG/JPG files using PIL
    - Detects transparency (alpha channel presence)
    - Extracts dominant colors as hex palette (top 5 colors)
    - Classifies texture category (blocks, items, entities, gui, environment) from path
    - Detects if texture is tileable (edge matching analysis)
    - Extracts animation frame count if animated PNG
    - Computes visual complexity score based on color variance
    Use ImageMetadata from schemas.multimodal_schema for the output model.
    Handle errors gracefully (return None for unreadable files).
  </action>
  <verify>
    <automated>python -c "
from ai_engine.utils.texture_metadata_extractor import TextureMetadataExtractor
import tempfile
import os

# Create test PNG
from PIL import Image
with tempfile.NamedTemporaryFile(suffix='.png', delete=False) as f:
    img = Image.new('RGBA', (64, 64), (255, 0, 0, 255))
    img.save(f.name)
    extractor = TextureMetadataExtractor()
    result = extractor.extract(f.name)
    os.unlink(f.name)
    assert result is not None
    assert result['width'] == 64
    assert result['height'] == 64
    print('Texture extractor test passed')
"</automated>
  </verify>
  <done>TextureMetadataExtractor extracts width, height, format, transparency, color palette, category from PNG/JPG files</done>
</task>

<task type="auto">
  <name>Task 2: 3D Model Documentation Extractor</name>
  <files>ai-engine/utils/model_metadata_extractor.py</files>
  <action>
    Create model_metadata_extractor.py with ModelMetadataExtractor class that:
    - Parses Bedrock .json model files (RP/models/*, RP/animations/*)
    - Extracts geometry definition (cube count, bone count)
    - Extracts animation data (animation names, length, loops)
    - Extracts material references (textures used)
    - Extracts parent model references (for variants)
    - Classifies model type (entity, block, item, animated)
    Create extract_model_metadata(file_path: str) -> dict function.
    Handle JSON parse errors gracefully.
  </action>
  <verify>
    <automated>python -c "
from ai_engine.utils.model_metadata_extractor import ModelMetadataExtractor, extract_model_metadata
import tempfile
import json

# Create test model JSON
model_data = {
    'minecraft:geometry': [{
        'description': {'identifier': 'geometry.test', 'texture_width': 64, 'texture_height': 64},
        'bones': [{'name': 'root', 'cubes': [{'size': [16, 16, 16]}]}]
    }]
}
with tempfile.NamedTemporaryFile(suffix='.json', delete=False, mode='w') as f:
    json.dump(model_data, f)
    result = extract_model_metadata(f.name)
    import os
    os.unlink(f.name)
    assert result is not None
    assert result['geometry_count'] == 1
    assert result['cube_count'] == 1
    print('Model extractor test passed')
"</automated>
  </verify>
  <done>ModelMetadataExtractor extracts geometry, animation, material references from Bedrock .json model files</done>
</task>

<task type="auto">
  <name>Task 3: Multi-Modal Search Engine</name>
  <files>ai-engine/search/multimodal_search_engine.py</files>
  <action>
    Create multimodal_search_engine.py with MultiModalSearchEngine class that:
    - Extends or composes HybridSearchEngine for base search
    - Supports content_type filtering (texture, model, code, text, documentation)
    - Implements modality-aware scoring: text/code results weighted higher for code queries, texture results higher for visual queries
    - Adds cross-modal expansion: if searching for code, also find related textures
    - Accepts query with explicit content_types filter
    - Returns results with modality information and cross-modal related items
    Use SearchQuery and SearchResult from schemas.multimodal_schema.
  </action>
  <verify>
    <automated>python -c "
from ai_engine.search.multimodal_search_engine import MultiModalSearchEngine
from schemas.multimodal_schema import SearchQuery, ContentType

engine = MultiModalSearchEngine()
query = SearchQuery(
    query_text='block texture stone',
    content_types=[ContentType.TEXT, ContentType.IMAGE],
    top_k=5
)
# Test that engine accepts query without error
print('MultiModalSearchEngine initialization test passed')
"</automated>
  </verify>
  <done>MultiModalSearchEngine enables searching across specific content types with modality-aware scoring</done>
</task>

<task type="auto">
  <name>Task 4: Cross-Modal Retriever</name>
  <files>ai-engine/search/cross_modal_retriever.py</files>
  <action>
    Create cross_modal_retriever.py with CrossModalRetriever class that:
    - Finds related content across different modalities
    - Given a code item → finds related textures (by extracting texture references)
    - Given a texture → finds related code (by finding code that references similar textures)
    - Uses embeddings to find semantically similar items across modalities
    - Implements find_related_across_modalities(document_id, target_modalities) method
    - Returns list of related documents with relationship type and confidence
    Store relationships in database for faster retrieval.
  </action>
  <verify>
    <automated>python -c "
from ai_engine.search.cross_modal_retriever import CrossModalRetriever

retriever = CrossModalRetriever()
# Test initialization
assert retriever is not None
print('CrossModalRetriever initialization test passed')
"</automated>
  </verify>
  <done>CrossModalRetriever finds related content across different modalities (code ↔ texture, text ↔ image)</done>
</task>

<task type="auto">
  <name>Task 5: Knowledge Base API Integration</name>
  <files>backend/src/api/knowledge_base.py</files>
  <action>
    Add new endpoints to backend/src/api/knowledge_base.py:
    - POST /knowledge-base/assets/texture: Upload and index texture file
    - POST /knowledge-base/assets/model: Upload and index 3D model file
    - GET /knowledge-base/search/multimodal: Search with content type filtering
    - GET /documents/{doc_id}/related-modal: Get related content across modalities
    
    Integrate TextureMetadataExtractor and ModelMetadataExtractor into the ingestion pipeline.
  </action>
  <verify>
    <automated>python -c "
# Check that new endpoints are properly defined in knowledge_base.py
import ast
with open('backend/src/api/knowledge_base.py') as f:
    content = f.read()
    assert 'texture' in content.lower() or 'multimodal' in content.lower()
    print('API integration check passed')
"</automated>
  </verify>
  <done>Knowledge base API supports texture/model indexing and multi-modal search</done>
</task>

<task type="auto">
  <name>Task 6: Unit Tests for Multi-Modal Search</name>
  <files>ai-engine/tests/test_multimodal_search.py</files>
  <action>
    Create comprehensive unit tests:
    - Test TextureMetadataExtractor: dimensions, transparency, color palette, category classification
    - Test ModelMetadataExtractor: geometry parsing, animation extraction, material references
    - Test MultiModalSearchEngine: content type filtering, modality-aware scoring
    - Test CrossModalRetriever: finding related content across modalities
    
    Target: 20+ passing tests.
  </action>
  <verify>
    <automated>python -m pytest ai-engine/tests/test_multimodal_search.py -v --tb=short 2>/dev/null || echo "Tests may need fixtures - manual verification required"</automated>
  </verify>
  <done>20+ unit tests passing for multi-modal search components</done>
</task>

</tasks>

<verification>
- [ ] TextureMetadataExtractor extracts all metadata fields correctly
- [ ] ModelMetadataExtractor parses Bedrock .json model files correctly
- [ ] MultiModalSearchEngine filters by content type and scores by modality
- [ ] CrossModalRetriever finds related content across modalities
- [ ] API endpoints accept texture/model files and return multi-modal results
- [ ] Unit tests pass (20+ tests)
</verification>

<success_criteria>
Multi-modal knowledge system is operational with:
- Texture metadata extraction working (dimensions, transparency, colors, category)
- 3D model documentation processing working (geometry, animations, materials)
- Multi-modal search working (filter by content type, modality-aware scoring)
- Cross-modal retrieval working (find related content across modalities)
- Unit tests passing (20+)
- Requirements RAG-8.1 and RAG-8.2 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/15-08-multi-modal-knowledge/15-08-01-SUMMARY.md`
</output>
