---
phase: 15-08-multi-modal-knowledge
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-engine/search/cross_modal_retriever.py
  - ai-engine/utils/texture_metadata_extractor.py
  - ai-engine/search/multimodal_search_engine.py
  - ai-engine/schemas/multimodal_schema.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "System can find related content across different modalities (code ↔ texture) using real embedding similarity"
    - "Texture metadata returns structured ImageMetadata model instead of raw dict"
    - "MultiModalSearchEngine uses real embedding-based search instead of fallback"
  artifacts:
    - path: "ai-engine/search/cross_modal_retriever.py"
      provides: "Cross-modal retrieval with real embedding-based similarity"
      contains: "embedding-based similarity instead of mock"
    - path: "ai-engine/utils/texture_metadata_extractor.py"
      provides: "Texture metadata as ImageMetadata model"
      contains: "returns ImageMetadata instance"
    - path: "ai-engine/search/multimodal_search_engine.py"
      provides: "Real embedding-based multi-modal search"
      contains: "No fallback to simple search"
---

<objective>
Close verification gaps from 15-08-01-VERIFICATION.md. Fix stub implementations that use mocks/fallbacks instead of real embedding-based functionality.

Purpose: Make multi-modal knowledge system actually work with real embeddings instead of placeholders.

Output: Working cross-modal retrieval, fixed texture extractor, real embedding search.
</objective>

<context>
@.planning/phases/15-08-multi-modal-knowledge/15-08-01-VERIFICATION.md
@.planning/phases/15-08-multi-modal-knowledge/15-08-01-SUMMARY.md
@ai-engine/schemas/multimodal_schema.py
@ai-engine/search/hybrid_search_engine.py

## Gap Details from VERIFICATION.md

### Gap 1: Cross-Modal Retriever uses mock relationships
- File: ai-engine/search/cross_modal_retriever.py
- Lines 138-174: _generate_mock_relationships creates placeholder data
- Fix needed: Use real embedding-based similarity search across modalities

### Gap 2: TextureMetadataExtractor doesn't use ImageMetadata model
- File: ai-engine/utils/texture_metadata_extractor.py
- Line 23: Import exists but never used - extract() returns Dict instead of ImageMetadata
- Fix needed: Return ImageMetadata instance instead of Dict

### Gap 3: MultiModalSearchEngine falls back to simple search
- File: ai-engine/search/multimodal_search_engine.py
- Lines 130-140: Falls back to _simple_search if hybrid search not available
- Fix needed: Use real embedding-based search end-to-end
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Cross-Modal Retriever to use real embeddings</name>
  <files>ai-engine/search/cross_modal_retriever.py</files>
  <read_first>
    - ai-engine/search/cross_modal_retriever.py
    - ai-engine/schemas/multimodal_schema.py
    - ai-engine/search/hybrid_search_engine.py
  </read_first>
  <action>
    Fix CrossModalRetriever to use real embedding-based similarity:

    1. Replace _generate_mock_relationships() method (lines 138-174):
       - Query the vector database for similar embeddings across modalities
       - Use the embedding generator to create embeddings for cross-modal similarity
       - Store real relationships in the database instead of placeholders

    2. Implement find_related_across_modalities() to:
       - Generate embedding for the source document
       - Search vector DB for similar embeddings in target modalities
       - Return actual related documents with similarity scores

    3. Fix store_relationship() (around line 301):
       - Store relationships in actual database, not just placeholder comments

    Key implementation:
    - Use HybridSearchEngine or direct vector DB queries for similarity
    - Query embeddings by modality filter
    - Return ranked results with confidence scores from embedding similarity
  </action>
  <verify>
    <automated>python -c "
from ai_engine.search.cross_modal_retriever import CrossModalRetriever

retriever = CrossModalRetriever()
# Verify find_related_across_modalities doesn't use mock data
import inspect
source = inspect.getsource(retriever.find_related_across_modalities)
assert 'mock' not in source.lower(), 'find_related_across_modalities still uses mock'
assert '_generate_mock' not in source, '_generate_mock_relationships still present'
print('CrossModalRetriever fixed: no mock relationships')
"</automated>
  </verify>
  <acceptance_criteria>
    - grep "def _generate_mock_relationships" cross_modal_retriever.py returns nothing
    - grep "def find_related_across_modalities" cross_modal_retriever.py shows real embedding queries
    - grep "def store_relationship" cross_modal_retriever.py shows actual DB storage, not placeholder
  </acceptance_criteria>
  <done>CrossModalRetriever uses real embedding-based similarity for cross-modal retrieval</done>
</task>

<task type="auto">
  <name>Task 2: Fix TextureMetadataExtractor to return ImageMetadata model</name>
  <files>ai-engine/utils/texture_metadata_extractor.py</files>
  <read_first>
    - ai-engine/utils/texture_metadata_extractor.py
    - ai-engine/schemas/multimodal_schema.py
  </read_first>
  <action>
    Fix TextureMetadataExtractor to return ImageMetadata instance:

    1. Check the ImageMetadata model in multimodal_schema.py for required fields:
       - width, height, format, transparency, colors, category, tileable, animation_frames, complexity

    2. Modify the extract() method to return ImageMetadata(...):
       - Instead of returning Dict[str, Any], return ImageMetadata instance
       - Map extracted fields to ImageMetadata model fields
       - Handle optional fields appropriately

    3. Keep the import that already exists at line 23 but actually USE it:
       - from ai_engine.schemas.multimodal_schema import ImageMetadata

    Example:
      return ImageMetadata(
          width=width,
          height=height,
          format=format,
          transparency=transparency,
          colors=colors,
          category=category,
          tileable=tileable,
          animation_frames=animation_frames,
          complexity=complexity
      )
  </action>
  <verify>
    <automated>python -c "
from ai_engine.utils.texture_metadata_extractor import TextureMetadataExtractor
from ai_engine.schemas.multimodal_schema import ImageMetadata
import tempfile
from PIL import Image

with tempfile.NamedTemporaryFile(suffix='.png', delete=False) as f:
    img = Image.new('RGBA', (64, 64), (255, 0, 0, 255))
    img.save(f.name)
    extractor = TextureMetadataExtractor()
    result = extractor.extract(f.name)
    import os
    os.unlink(f.name)
    
    # Check it's an ImageMetadata instance
    assert isinstance(result, ImageMetadata), f'Expected ImageMetadata, got {type(result)}'
    assert result.width == 64
    assert result.height == 64
    print('TextureMetadataExtractor returns ImageMetadata instance')
"</automated>
  </verify>
  <acceptance_criteria>
    - grep "return ImageMetadata" texture_metadata_extractor.py returns the return statement
    - grep "isinstance.*ImageMetadata" test shows ImageMetadata type check passes
    - No Dict return type in extract() method signature
  </acceptance_criteria>
  <done>TextureMetadataExtractor.extract() returns ImageMetadata instance instead of Dict</done>
</task>

<task type="auto">
  <name>Task 3: Fix MultiModalSearchEngine to use real embeddings</name>
  <files>ai-engine/search/multimodal_search_engine.py</files>
  <read_first>
    - ai-engine/search/multimodal_search_engine.py
    - ai-engine/search/hybrid_search_engine.py
  </read_first>
  <action>
    Fix MultiModalSearchEngine to use real embedding-based search:

    1. Find the _simple_search fallback (around lines 130-140):
       - Remove or refactor the fallback to simple keyword search
       - The search should always use embedding-based similarity

    2. Ensure search() method:
       - Generates embeddings for the query using embedding generator
       - Queries vector DB with embedding for semantic similarity
       - Combines with hybrid search (semantic + keyword) for best results
       - Falls back gracefully only if embeddings completely unavailable

    3. Check integration with HybridSearchEngine:
       - Ensure MultiModalSearchEngine properly uses HybridSearchEngine
       - The hybrid approach should work end-to-end without fallback

    Key: Remove "if not hybrid_available" fallback paths. Always use embeddings.
  </action>
  <verify>
    <automated>python -c "
from ai_engine.search.multimodal_search_engine import MultiModalSearchEngine
import inspect

engine = MultiModalSearchEngine()
source = inspect.getsource(engine.search)

# Check no simple fallback in search method
assert '_simple_search' not in source, 'search still uses _simple_search fallback'
assert 'fallback' not in source.lower() or 'hybrid' in source.lower(), 'search has fallback logic'

print('MultiModalSearchEngine: real embedding-based search, no fallback')
"</automated>
  </verify>
  <acceptance_criteria>
    - grep "_simple_search" multimodal_search_engine.py returns nothing
    - grep "def search" multimodal_search_engine.py shows embedding generation and vector DB query
    - No "if not.*available" fallback patterns in search method
  </acceptance_criteria>
  <done>MultiModalSearchEngine.search() uses real embedding-based search without fallback</done>
</task>

<task type="auto">
  <name>Task 4: Wire ModelMetadataExtractor to MultiModalDocument schema</name>
  <files>ai-engine/utils/model_metadata_extractor.py</files>
  <read_first>
    - ai-engine/utils/model_metadata_extractor.py
    - ai-engine/schemas/multimodal_schema.py
  </read_first>
  <action>
    Fix the broken key link: ModelMetadataExtractor should use MultiModalDocument from multimodal_schema.

    1. Add import: from ai_engine.schemas.multimodal_schema import MultiModalDocument

    2. Modify extract_model_metadata() to return MultiModalDocument instance:
       - Create MultiModalDocument with type=ContentType.MODEL
       - Include all extracted metadata in the document content/fields
       - Return MultiModalDocument instead of plain dict

    3. This addresses the verification finding that "Model→schema link broken"
  </action>
  <verify>
    <automated>python -c "
from ai_engine.utils.model_metadata_extractor import ModelMetadataExtractor, extract_model_metadata
from ai_engine.schemas.multimodal_schema import MultiModalDocument, ContentType
import tempfile
import json

model_data = {
    'minecraft:geometry': [{
        'description': {'identifier': 'geometry.test', 'texture_width': 64, 'texture_height': 64},
        'bones': [{'name': 'root', 'cubes': [{'size': [16, 16, 16]}]}]
    }]
}
with tempfile.NamedTemporaryFile(suffix='.json', delete=False, mode='w') as f:
    json.dump(model_data, f)
    result = extract_model_metadata(f.name)
    import os
    os.unlink(f.name)
    
# Check it returns MultiModalDocument
assert isinstance(result, MultiModalDocument), f'Expected MultiModalDocument, got {type(result)}'
assert result.content_type == ContentType.MODEL
print('ModelMetadataExtractor returns MultiModalDocument instance')
"</automated>
  </verify>
  <acceptance_criteria>
    - grep "from.*multimodal_schema import.*MultiModalDocument" model_metadata_extractor.py shows import
    - grep "return MultiModalDocument" model_metadata_extractor.py shows return statement
    - Test verifies isinstance(result, MultiModalDocument)
  </acceptance_criteria>
  <done>ModelMetadataExtractor returns MultiModalDocument instance wired to schema</done>
</task>

</tasks>

<verification>
- [ ] CrossModalRetriever uses real embedding-based similarity (no mocks)
- [ ] TextureMetadataExtractor returns ImageMetadata instance (not Dict)
- [ ] MultiModalSearchEngine uses embedding-based search (no fallback)
- [ ] ModelMetadataExtractor returns MultiModalDocument (schema wired)
</verification>

<success_criteria>
All 4 verification gaps closed:
1. Cross-modal retrieval works with real embeddings
2. Texture extractor uses structured ImageMetadata model
3. Search engine uses real embeddings without fallback
4. Model extractor wired to MultiModalDocument schema
</success_criteria>

<output>
After completion, create `.planning/phases/15-08-multi-modal-knowledge/15-08-02-SUMMARY.md`
</output>
