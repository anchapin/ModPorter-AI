---
phase: 15-07-advanced-rag-pipeline
plan: "03"
type: execute
wave: 3
depends_on:
  - "15-07-02"
files_modified: []
autonomous: true
requirements:
  - RAG-7.1
  - RAG-7.2
  - RAG-7.3

must_haves:
  truths:
    - "Pipeline results are cached for repeated queries"
    - "Complex queries can be processed in batches"
    - "Performance meets SLA targets"
    - "Full integration tests pass"
  artifacts:
    - path: "ai-engine/search/pipeline_cache.py"
      provides: "Caching layer for pipeline results"
      min_lines: 80
    - path: "tests/search/test_rag_integration.py"
      provides: "Integration tests"
      min_tests: 20
  key_links:
    - from: "rag_pipeline.py"
      to: "pipeline_cache.py"
      via: "import and cache decorator"
---

<objective>
Add performance optimization with caching and comprehensive integration tests.
</objective>

<context>
@.planning/phases/15-07-advanced-rag-pipeline/15-07-02-PLAN.md
@ai-engine/search/rag_pipeline.py
@ai-engine/search/query_rewriter.py
@ai-engine/search/adaptive_fusion.py

# Context from Wave 2:
# - QueryRewriter exists
# - AdaptiveFusion exists  
# - API endpoint exists
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Pipeline Cache</name>
  <files>ai-engine/search/pipeline_cache.py</files>
  <action>
Create `ai-engine/search/pipeline_cache.py` with:

1. **PipelineCache class**:
   - `__init__(backend: CacheBackend = "memory", ttl: int = 3600)`
   - `get(key: str) -> Optional[CachedResult]`
   - `set(key: str, result: CachedResult) -> None`
   - `invalidate(pattern: str = None) -> None`
   
2. **CacheBackend options**:
   - "memory" - In-memory LRU cache (default)
   - "redis" - Redis-backed for distributed systems
   
3. **CachedResult dataclass**:
   - `results: List[SearchResult]`
   - `query_analysis: QueryAnalysis`
   - `timestamp: datetime`
   - `ttl: int`

4. **Cache key generation**:
   - Normalize query (lowercase, strip whitespace)
   - Include config hash for different configs
   - Support cache invalidation by pattern

5. **Integration with RAGPipeline**:
   - Add cache parameter to PipelineConfig
   - Check cache before executing stages
   - Store results after pipeline completes

6. **Performance features**:
   - Configurable TTL (default 1 hour)
   - Max cache size with LRU eviction
   - Cache statistics (hits, misses, size)

Support Redis backend for production deployments.
  </action>
  <verify>
    <automated>python -c "from ai_engine.search.pipeline_cache import PipelineCache, MemoryCache; c = PipelineCache(); print('Import OK')"</automated>
  </verify>
  <done>PipelineCache can be instantiated with memory backend</done>
</task>

<task type="auto">
  <name>Task 2: Add Caching to RAG Pipeline</name>
  <files>ai-engine/search/rag_pipeline.py</files>
  <action>
Update `ai-engine/search/rag_pipeline.py` to integrate caching:

1. **Update PipelineConfig**:
   - Add `cache_enabled: bool = True`
   - `cache_ttl: int = 3600`
   - `cache_backend: str = "memory"`

2. **Update RAGPipeline**:
   - Initialize PipelineCache in __init__
   - Check cache at start of search()
   - Store results in cache at end of search()
   - Add cache statistics to PipelineResult

3. **Cache key strategy**:
   - Use normalized query + config hash
   - Different configs = different cache entries
   
4. **Cache invalidation**:
   - Add method to clear cache
   - Support pattern-based invalidation

Test cache hit/miss behavior.
  </action>
  <verify>
    <automated>python -c "
from ai_engine.search.rag_pipeline import RAGPipeline, PipelineConfig
p = RAGPipeline(PipelineConfig(cache_enabled=True))
print('Cache integration OK')
"</automated>
  </verify>
  <done>Pipeline integrates caching without errors</done>
</task>

<task type="auto">
  <name>Task 3: Create Integration Tests</name>
  <files>tests/search/test_rag_integration.py</files>
  <action>
Create `tests/search/test_rag_integration.py` with comprehensive tests:

1. **End-to-end pipeline tests**:
   - Test full pipeline with real components
   - Test with mocked external services
   - Test query rewriting flow
   - Test multi-stage reranking
   - Test adaptive fusion

2. **Performance tests**:
   - Test cache hit/miss
   - Test timing for simple queries (<100ms target)
   - Test timing for complex queries (<500ms target)

3. **Edge case tests**:
   - Test empty query handling
   - Test very long queries
   - Test with special characters
   - Test cache invalidation
   - Test concurrent requests

4. **API integration tests**:
   - Test endpoint with valid request
   - Test endpoint validation
   - Test error handling

Use pytest markers:
- `@pytest.mark.integration` for full integration tests
- `@pytest.mark.performance` for performance tests

Target: 20+ tests passing
  </action>
  <verify>
    <automated>pytest tests/search/test_rag_integration.py -v --tb=short 2>&1 | tail -25</automated>
  </verify>
  <done>20+ integration tests passing</done>
</task>

</tasks>

<verification>
- [ ] PipelineCache imports and works
- [ ] Pipeline caching integration complete
- [ ] 20+ integration tests passing
- [ ] Performance targets met
</verification>

<success_criteria>
Complete Advanced RAG Pipeline with caching and all tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/15-07-advanced-rag-pipeline/15-07-01-SUMMARY.md` and update roadmap.
</output>
