---
phase: 15-07-advanced-rag-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - RAG-7.1
  - RAG-7.2
  - RAG-7.3

must_haves:
  truths:
    - "User queries go through configurable multi-stage pipeline"
    - "Reranking applies multiple strategies in sequence"
    - "Query expansion adds relevant terms before search"
    - "Results are fused from multiple sources intelligently"
  artifacts:
    - path: "ai-engine/search/rag_pipeline.py"
      provides: "Main RAG pipeline orchestrator"
      min_lines: 200
    - path: "ai-engine/search/multi_stage_reranker.py"
      provides: "Multi-stage reranking implementation"
      min_lines: 150
    - path: "tests/search/test_rag_pipeline.py"
      provides: "Unit tests for pipeline"
      min_tests: 15
  key_links:
    - from: "rag_pipeline.py"
      to: "hybrid_search_engine.py"
      via: "import and calls search()"
    - from: "rag_pipeline.py"
      to: "reranking_engine.py"
      via: "import and stage configuration"
---

<objective>
Create the Advanced RAG Pipeline - a unified orchestrator that combines query processing, search, reranking, and fusion into a coherent multi-stage system.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-07-advanced-rag-pipeline/CONTEXT.md
@ai-engine/search/hybrid_search_engine.py
@ai-engine/search/reranking_engine.py
@ai-engine/search/query_expansion.py

# Key interfaces from existing code:
# - HybridSearchEngine.search(query, top_k, mode, ...) -> List[SearchResult]
# - ReRankingStrategy enum: CROSS_ENCODER, FEATURE_BASED, NEURAL_RERANKER, ENSEMBLE, CONTEXTUAL
# - ExpansionStrategy enum: SYNONYM, CONTEXTUAL, DOMAIN, SEMANTIC, HISTORICAL
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RAG Pipeline Orchestrator</name>
  <files>ai-engine/search/rag_pipeline.py</files>
  <action>
Create `ai-engine/search/rag_pipeline.py` with:

1. **RAGPipeline class** - Main orchestrator:
   - `__init__(config: PipelineConfig)` - Initialize with stages
   - `search(query: str, **kwargs) -> PipelineResult` - Execute full pipeline
   - `add_stage(stage: PipelineStage)` - Add processing stage
   
2. **PipelineConfig dataclass**:
   - `enable_query_expansion: bool = True`
   - `enable_reranking: bool = True`
   - `reranking_stages: List[str] = ["feature", "cross_encoder"]`
   - `fusion_strategy: str = "reciprocal_rank"`
   - `max_results: int = 20`

3. **PipelineStage protocol**:
   - Abstract interface for pipeline stages
   - `process(input: Any) -> Any`
   - `get_config() -> Dict`

4. **PipelineResult dataclass**:
   - `results: List[SearchResult]`
   - `query_analysis: QueryAnalysis`
   - `expansion_metadata: Dict`
   - `reranking_stages_applied: List[str]`
   - `timing: Dict[str, float]`

5. **QueryAnalysis dataclass**:
   - `original_query: str`
   - `rewritten_query: Optional[str]`
   - `expanded_terms: List[str]`
   - `query_type: QueryType`
   - `complexity: QueryComplexity`

Integrate with existing:
- HybridSearchEngine for initial search
- QueryExpander for query expansion
- FeatureBasedReRanker and CrossEncoderReranker for reranking

Use existing RankingStrategy.RECIPROCAL_RANK_FUSION for final fusion.
  </action>
  <verify>
    <automated>python -c "from ai_engine.search.rag_pipeline import RAGPipeline, PipelineConfig; p = RAGPipeline(PipelineConfig()); print('Import OK')"</automated>
  </verify>
  <done>RAGPipeline can be instantiated and imported without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create Multi-Stage Reranker</name>
  <files>ai-engine/search/multi_stage_reranker.py</files>
  <action>
Create `ai-engine/search/multi_stage_reranker.py` with:

1. **MultiStageReranker class**:
   - `__init__(stages: List[RerankStageConfig])` - Configure stages
   - `rerank(query: str, results: List[SearchResult]) -> List[SearchResult]`
   - `add_stage(name: str, strategy: ReRankingStrategy, config: Dict)`
   
2. **RerankStageConfig dataclass**:
   - `name: str`
   - `strategy: ReRankingStrategy`
   - `top_k: int = None` - How many to keep after this stage
   - `weight: float = 1.0` - Weight in ensemble
   - `model_name: str = None` - For cross-encoder
   
3. **Stage execution**:
   - Sequential execution of each stage
   - Each stage can reduce or expand result set
   - Scores are combined using configured strategy
   - Track which stage made what changes

4. **Built-in stage configurations**:
   - `LIGHTWEIGHT = [feature_based]` - Fast, no LLM
   - `STANDARD = [feature_based, cross_encoder]` - Balanced
   - `COMPREHENSIVE = [feature_based, neural, cross_encoder, ensemble]` - Best quality

Import and use:
- FeatureBasedReRanker from reranking_engine
- CrossEncoderReranker from reranking_engine  
- EnsembleReranker from reranking_engine

Support early exit if scores converge.
  </action>
  <verify>
    <automated>python -c "from ai_engine.search.multi_stage_reranker import MultiStageReranker, RerankStageConfig, STANDARD; print('Import OK')"</automated>
  </verify>
  <done>MultiStageReranker can be instantiated and imported</done>
</task>

<task type="auto">
  <name>Task 3: Write Unit Tests for RAG Pipeline</name>
  <files>tests/search/test_rag_pipeline.py</files>
  <action>
Create `tests/search/test_rag_pipeline.py` with tests for:

1. **Pipeline initialization tests**:
   - Test default configuration
   - Test custom stage configuration
   - Test stage ordering

2. **Pipeline execution tests**:
   - Test full pipeline with mock search
   - Test pipeline with disabled expansion
   - Test pipeline with disabled reranking

3. **Multi-stage reranker tests**:
   - Test sequential reranking
   - Test stage weight combination
   - Test early exit on convergence

4. **Query analysis tests**:
   - Test query type detection
   - Test complexity scoring

Use pytest fixtures for mocking:
- Mock HybridSearchEngine.search()
- Mock reranker classes
- Use sample SearchResult objects

Ensure tests pass: `pytest tests/search/test_rag_pipeline.py -v`
  </action>
  <verify>
    <automated>pytest tests/search/test_rag_pipeline.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>At least 15 tests passing</done>
</task>

</tasks>

<verification>
- [ ] RAGPipeline imports without errors
- [ ] MultiStageReranker imports without errors
- [ ] Pipeline can execute end-to-end with mocked components
- [ ] All 15+ unit tests pass
</verification>

<success_criteria>
Pipeline orchestrates multi-stage reranking with configurable stages
</success_criteria>

<output>
After completion, create `.planning/phases/15-07-advanced-rag-pipeline/15-07-01-SUMMARY.md`
</output>
