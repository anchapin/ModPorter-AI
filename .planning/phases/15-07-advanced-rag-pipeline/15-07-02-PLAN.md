---
phase: 15-07-advanced-rag-pipeline
plan: "02"
type: execute
wave: 2
depends_on:
  - "15-07-01"
files_modified: []
autonomous: true
requirements:
  - RAG-7.2
  - RAG-7.3

must_haves:
  truths:
    - "Ambiguous queries are rewritten for clarity"
    - "Query type determines fusion strategy"
    - "Results are intelligently combined from multiple sources"
    - "API endpoint exposes pipeline functionality"
  artifacts:
    - path: "ai-engine/search/query_rewriter.py"
      provides: "LLM-based query rewriting"
      min_lines: 100
    - path: "ai-engine/search/adaptive_fusion.py"
      provides: "Query-type-aware score fusion"
      min_lines: 100
    - path: "backend/app/api/v1/search/rag.py"
      provides: "REST API endpoint for pipeline"
      min_lines: 80
  key_links:
    - from: "rag_pipeline.py"
      to: "query_rewriter.py"
      via: "import and call rewrite()"
    - from: "rag_pipeline.py"
      to: "adaptive_fusion.py"
      via: "import and call fuse()"
---

<objective>
Add query rewriting and adaptive fusion to the RAG pipeline.
</objective>

<context>
@.planning/phases/15-07-advanced-rag-pipeline/15-07-01-PLAN.md
@ai-engine/search/rag_pipeline.py
@ai-engine/search/query_complexity_analyzer.py

# Context from Wave 1:
# - RAGPipeline orchestrator exists
# - MultiStageReranker exists
# - Tests for pipeline exist
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Query Rewriter</name>
  <files>ai-engine/search/query_rewriter.py</files>
  <action>
Create `ai-engine/search/query_rewriter.py` with:

1. **QueryRewriter class**:
   - `__init__(llm_client=None, enabled=True)` - Optional LLM for rewriting
   - `rewrite(query: str, context: Dict = None) -> RewriteResult`
   - `should_rewrite(query: str, complexity: QueryComplexity) -> bool`
   
2. **RewriteResult dataclass**:
   - `original_query: str`
   - `rewritten_query: str`
   - `confidence: float`
   - `rewrite_type: RewriteType`
   - `explanation: str`

3. **RewriteType enum**:
   - CLARIFICATION - Expanding abbreviated terms
   - SPECIFICATION - Making vague queries specific
   - DECOMPOSITION - Splitting complex queries
   - NONE - No rewrite needed

4. **Rewrite strategies**:
   - **Rule-based**: Expand common abbreviations, fix typos
   - **Pattern-based**: Apply template transformations
   - **LLM-based** (optional): Use LLM for complex rewrites

5. **Integration with query_complexity_analyzer**:
   - Only rewrite queries above complexity threshold
   - Use analysis to guide rewrite direction

Support disable for performance-sensitive use cases.
  </action>
  <verify>
    <automated>python -c "from ai_engine.search.query_rewriter import QueryRewriter, RewriteResult; r = QueryRewriter(enabled=False); print('Import OK')"</automated>
  </verify>
  <done>QueryRewriter can be instantiated with and without LLM</done>
</task>

<task type="auto">
  <name>Task 2: Create Adaptive Fusion</name>
  <files>ai-engine/search/adaptive_fusion.py</files>
  <action>
Create `ai-engine/search/adaptive_fusion.py` with:

1. **AdaptiveFusion class**:
   - `__init__(default_strategy: str = "reciprocal_rank")`
   - `fuse(results: Dict[str, List[SearchResult]], query_type: QueryType) -> List[SearchResult]`
   - `select_strategy(query_type: QueryType, query_complexity: QueryComplexity) -> str`
   
2. **Fusion strategies by query type**:
   - **INFORMATIONAL**: Semantic-first (vector scores weighted higher)
   - **NAVIGATIONAL**: Keyword-first (BM25 scores weighted higher)
   - **TRANSACTIONAL**: Hybrid balanced
   - **COMPLEX**: Ensemble with all signals
   - **SIMPLE**: Fast single-source

3. **Score normalization**:
   - Min-max normalization across different scorers
   - Handle missing scores gracefully
   - Apply query-type weights

4. **Built-in strategies**:
   - `RECIPROCAL_RANK_FUSION` - Existing from HybridSearchEngine
   - `WEIGHTED_SUM` - Configurable weights
   - `SCORE_AVERAGING` - Equal weights
   - `CONFIDENCE_WEIGHTED` - Based on score confidence

5. **Query type detection**:
   - Use query_complexity_analyzer for classification
   - Map complexity to fusion strategy

Integrate with existing HybridSearchEngine.RankingStrategy.
  </action>
  <verify>
    <automated>python -c "from ai_engine.search.adaptive_fusion import AdaptiveFusion; f = AdaptiveFusion(); print('Import OK')"</automated>
  </verify>
  <done>AdaptiveFusion can be instantiated and imported</done>
</task>

<task type="auto">
  <name>Task 3: Create RAG Search API Endpoint</name>
  <files>backend/app/api/v1/search/rag.py</files>
  <action>
Create `backend/app/api/v1/search/rag.py` with:

1. **FastAPI endpoint**:
   ```python
   @router.post("/rag")
   async def rag_search(request: RAGSearchRequest) -> RAGSearchResponse
   ```

2. **Request model** (RAGSearchRequest):
   - `query: str`
   - `top_k: int = 20`
   - `enable_rewrite: bool = True`
   - `enable_rerank: bool = True`
   - `rerank_stages: List[str] = ["feature", "cross_encoder"]`
   - `fusion_strategy: str = "adaptive"`
   - `filters: Optional[Dict] = None`

3. **Response model** (RAGSearchResponse):
   - `results: List[SearchResult]`
   - `query_analysis: QueryAnalysis`
   - `rewritten_query: Optional[str]`
   - `timing: Dict[str, float]`
   - `stages_applied: List[str]`

4. **Integration**:
   - Import and use RAGPipeline from ai-engine
   - Handle errors gracefully with proper HTTP status codes
   - Add request validation
   - Include timing information for debugging

5. **Configuration**:
   - Support pipeline config via request
   - Use sensible defaults
   - Allow feature flags

Add to existing router in backend/app/api/v1/search/__init__.py
  </action>
  <verify>
    <automated>python -c "from backend.app.api.v1.search.rag import router; print('Router import OK')"</automated>
  </verify>
  <done>API endpoint can be imported and registered</done>
</task>

</tasks>

<verification>
- [ ] QueryRewriter imports without errors
- [ ] AdaptiveFusion imports without errors  
- [ ] API endpoint registered successfully
- [ ] Integration with RAGPipeline complete
</verification>

<success_criteria>
Pipeline includes query rewriting and adaptive fusion with API exposure
</success_criteria>

<output>
After completion, create `.planning/phases/15-07-advanced-rag-pipeline/15-07-02-SUMMARY.md`
</output>
