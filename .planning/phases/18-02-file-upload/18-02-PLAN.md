---
phase: "18-02"
plan: "01"
type: execute
wave: 1
depends_on:
  - "18-01"
files_modified:
  - backend/src/api/upload.py
  - backend/src/services/file_handler.py
  - backend/src/core/storage.py
autonomous: true
requirements:
  - INF-18-02
---

<objective>
Implement file upload system for handling JAR files, including validation, storage, and processing pipeline.
</objective>

<context>
@backend/src/api/ (existing API)
@backend/src/core/redis.py (from 18-01)

# Required Features
- JAR file upload with validation
- Secure file storage
- Chunked upload support
- File processing pipeline
</context>

<tasks>

<task type="auto">
  <name>Task 1: File Upload API</name>
  <files>backend/src/api/upload.py</files>
<read_first>
    - backend/src/main.py (existing entry)
  </read_first>
<action>
Create file upload endpoints:

1. **Upload endpoint**:
   - POST /api/v1/upload - Upload JAR file
   - Accept multipart/form-data
   - Validate file type (application/java-archive, application/zip)
   - Return job_id for tracking

2. **Upload with chunking**:
   - Support large file chunked uploads
   - Reassemble chunks server-side

3. **Status endpoint**:
   - GET /api/v1/upload/{job_id} - Check upload status
</action>
<verify>
<automated>python3 -c "import sys; sys.path.insert(0, 'backend/src'); from api import upload; print('Upload API OK')"</automated>
</verify>
<done>File upload endpoints working</done>
</task>

<task type="auto">
  <name>Task 2: File Handler Service</name>
  <files>backend/src/services/file_handler.py</files>
<action>
Create file handling service:

1. **JAR validation**:
   - Verify JAR/ZIP structure
   - Check for required manifest
   - Validate mod metadata

2. **File processing**:
   - Extract metadata from JAR
   - Identify mod loader (Forge/Fabric/NeoForge)
   - Parse mod info (modid, version, name)

3. **Virus scanning placeholder**:
   - Stub for future integration
</action>
<verify>
<automated>python3 -c "import sys; sys.path.insert(0, 'backend/src'); from services import file_handler; print('File handler OK')"</automated>
</verify>
<done>File handler service complete</done>
</task>

<task type="auto">
  <name>Task 3: Storage Configuration</name>
  <files>backend/src/core/storage.py</files>
<action>
Create storage abstraction:

1. **Storage backend**:
   - Local file storage (development)
   - S3-compatible storage (production)
   - Abstract interface for swapping

2. **File organization**:
   - /uploads/{user_id}/{job_id}/original.jar
   - /processing/{job_id}/
   - /results/{job_id}/

3. **Cleanup**:
   - TTL-based cleanup of old files
   - Manual cleanup endpoint
</action>
<verify>
<automated>python3 -c "import sys; sys.path.insert(0, 'backend/src'); from core import storage; print('Storage OK')"</automated>
</verify>
<done>Storage configuration complete</done>
</task>

<task type="auto">
  <name>Task 4: Integration Tests</name>
  <files>backend/tests/test_upload.py</files>
<action>
Create upload tests:

1. **Upload tests** (4 tests)
2. **Validation tests** (4 tests)
3. **Storage tests** (3 tests)
</action>
<verify>
<automated>python3 -m pytest backend/tests/test_upload.py -v --tb=short 2>&1 | tail -20</automated>
</verify>
<done>11 unit tests passing</done>
</task>

</tasks>

<verification>
<automated>python3 -c "from api.upload import router; from services.file_handler import FileHandler; from core.storage import StorageManager; print('All imports OK')"</automated>

<success_criteria>
- [ ] Upload API with validation
- [ ] File handler service
- [ ] Storage abstraction
- [ ] 11 unit tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/18-02-file-upload/18-02-SUMMARY.md`
</output>