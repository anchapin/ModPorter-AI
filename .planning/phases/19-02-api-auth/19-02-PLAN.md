---
phase: "19-02"
plan: "01"
type: execute
wave: 1
depends_on:
  - "19-01"
files_modified:
  - backend/src/api/auth.py
  - backend/src/core/auth.py
  - backend/src/db/models.py
  - backend/tests/test_auth.py
autonomous: true
requirements:
  - UI-19-02
---

<objective>
Implement API authentication system with JWT tokens, user registration, and login.
</objective>

<context>
@backend/src/api/ (existing API)
@backend/src/core/config.py (existing config)

# Required Features
- User registration
- JWT authentication
- Password hashing
- Token refresh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth Core</name>
  <files>backend/src/core/auth.py</files>
<action>
Create authentication core with JWT tokens, password hashing with bcrypt, and token generation/validation
</action>
<verify>
<automated>python3 -c "from core.auth import AuthManager; print('Auth core OK')"</automated>
</verify>
<done>Auth core complete</done>
</task>

<task type="auto">
  <name>Task 2: Auth API</name>
  <files>backend/src/api/auth.py</files>
<action>
Create auth endpoints: POST /register, POST /login, POST /refresh, POST /logout
</action>
<verify>
<automated>python3 -c "from api import auth; print('Auth API OK')"</automated>
</verify>
<done>Auth API complete</done>
</task>

<task type="auto">
  <name>Task 3: User Models</name>
  <files>backend/src/db/models.py</files>
<action>
Create User model with id, email, password_hash, created_at fields
</action>
<verify>
<automated>python3 -c "from db.models import User; print('User model OK')"</automated>
</verify>
<done>User models complete</done>
</task>

<task type="auto">
  <name>Task 4: Unit Tests</name>
  <files>backend/tests/test_auth.py</files>
<action>
Create 12 unit tests: password hashing (3), JWT (4), API endpoints (5)
</action>
<verify>
<automated>python3 -m pytest backend/tests/test_auth.py -v --tb=short 2>&1 | tail -15</automated>
</verify>
<done>12 tests passing</done>
</task>

</tasks>

<verification>
<automated>python3 -c "from core.auth import AuthManager; from api import auth; from db.models import User; print('All imports OK')"</automated>

<success_criteria>
- [ ] Auth core with JWT and bcrypt
- [ ] Auth API endpoints (register, login, refresh, logout)
- [ ] User model
- [ ] 12 unit tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/19-02-api-auth/19-02-SUMMARY.md`
</output>