---
phase: "18-04"
plan: "01"
type: execute
wave: 1
depends_on:
  - "18-03"
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/cd.yml
  - Dockerfile
  - backend/Dockerfile
autonomous: true
requirements:
  - INF-18-04
---

<objective>
Implement CI/CD pipeline with GitHub Actions for automated testing, building, and deployment.
</objective>

<context>
@backend/src/ (existing backend)
@ai-engine/ (existing ai-engine)

# Pipeline Requirements
- GitHub Actions workflows
- Automated testing
- Docker image building
- Deployment automation
</context>

<tasks>

<task type="auto">
  <name>Task 1: CI Workflow</name>
  <files>.github/workflows/ci.yml</files>
<read_first>
    - ai-engine/tests/ (existing tests)
  </read_first>
<action>
Create CI workflow:

1. **Triggers**:
   - Push to main
   - Pull requests

2. **Jobs**:
   - lint: Code linting (ruff, mypy)
   - test-backend: Backend unit tests
   - test-ai: AI engine tests
   - test-integration: Integration tests

3. **Matrix**:
   - Python versions: 3.11, 3.12
   - Node versions: 18, 20
</action>
<verify>
<automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); print('CI YAML valid')"</automated>
</verify>
<done>CI workflow complete</done>
</task>

<task type="auto">
  <name>Task 2: CD Workflow</name>
  <files>.github/workflows/cd.yml</files>
<action>
Create CD workflow:

1. **Triggers**:
   - Push to main (after CI passes)
   - Release tags

2. **Jobs**:
   - build: Docker image build
   - test-staging: Deploy to staging
   - deploy: Deploy to production

3. **Docker**:
   - Multi-platform builds
   - Image push to registry
   - Tags: latest, commit SHA
</action>
<verify>
<automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/cd.yml')); print('CD YAML valid')"</automated>
</verify>
<done>CD workflow complete</done>
</task>

<task type="auto">
  <name>Task 3: Dockerfiles</name>
<files>Dockerfile, backend/Dockerfile</files>
<action>
Create Dockerfiles:

1. **Root Dockerfile**:
   - Multi-stage build
   - Production ready

2. **Backend Dockerfile**:
   - Python 3.12
   - uvicorn server
   - Health check endpoint
</action>
<verify>
<automated>docker build --check -f Dockerfile . 2>&1 | head -5 || docker buildx build --check -f Dockerfile . 2>&1 | head -5 || echo "Docker build check needs docker daemon"</automated>
</verify>
<done>Dockerfiles complete</done>
</task>

<task type="auto">
  <name>Task 4: GitHub Actions Tests</name>
<files>.github/tests/test_workflows.py</files>
<action>
Create workflow validation tests:

1. **YAML validation** (3 tests)
2. **Job dependencies** (2 tests)
3. **Matrix config** (2 tests)
</action>
<verify>
<automated>python3 -m pytest .github/tests/ -v --tb=short 2>&1 | tail -15</automated>
</verify>
<done>7 workflow tests passing</done>
</task>

</tasks>

<verification>
<automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); yaml.safe_load(open('.github/workflows/cd.yml')); print('All workflows valid')"</automated>

<success_criteria>
- [ ] CI workflow with test jobs
- [ ] CD workflow with build/deploy
- [ ] Dockerfiles for production
- [ ] 7 workflow tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/18-04-ci-cd-pipeline/18-04-SUMMARY.md`
</output>