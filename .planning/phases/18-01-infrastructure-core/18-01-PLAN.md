---
phase: "18-01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - backend/src/db/migrations
  - backend/src/core/config.py
autonomous: true
requirements:
  - INF-18-01
---

<objective>
Set up core infrastructure including Docker Compose, PostgreSQL with pgvector, Redis, and basic monitoring for portkit.
</objective>

<context>
@backend/ (existing backend code)
@ai-engine/ (existing AI engine)

# Current State
The project has ai-engine and backend directories but lacks containerization and production-ready infrastructure.

# Required Infrastructure
- PostgreSQL 15 with pgvector for vector storage
- Redis 7 for job queue and caching
- Docker Compose for local development
- Basic health check endpoints
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Docker Compose Configuration</name>
  <files>docker-compose.yml</files>
  <read_first>
    - backend/src/main.py (existing entry point)
  </read_first>
<action>
Create comprehensive Docker Compose setup:

1. **Services**:
   - postgres: PostgreSQL 15 with pgvector
   - redis: Redis 7 for job queue
   - backend: FastAPI application
   - worker: Background job processor
   - prometheus: Metrics collection
   - grafana: Dashboards (optional)

2. **Configuration**:
   - Environment variables for all configs
   - Named volumes for data persistence
   - Health checks for each service
   - Network isolation

3. **Development**:
   - Hot reload for backend
   - Volume mounting for code
   - Debug ports exposed
</action>
<verify>
<automated>docker-compose config 2>&1 | head -20</automated>
</verify>
<done>Docker Compose with all required services</done>
</task>

<task type="auto">
  <name>Task 2: Database Schema with pgvector</name>
  <files>backend/src/db/schema.sql</files>
  <read_first>
    - backend/ (check existing DB code)
  </read_first>
<action>
Create database schema:

1. **Core tables**:
   - users: User accounts and auth
   - jobs: Conversion job tracking
   - conversions: Conversion history
   - patterns: RAG pattern storage

2. **pgvector tables**:
   - document_chunks: Vector embeddings
   - search_index: Vector search

3. **Migrations**:
   - SQL migration files
   - pgvector extension setup
</action>
<verify>
<automated>ls -la backend/src/db/</automated>
</verify>
<done>Complete database schema with pgvector</done>
</task>

<task type="auto">
  <name>Task 3: Redis Configuration</name>
  <files>backend/src/core/redis.py</files>
  <read_first>
    - backend/src/core/config.py (existing config)
  </read_first>
<action>
Set up Redis integration:

1. **Redis client**:
   - Connection pooling
   - Serialization (JSON/msgpack)
   - Error handling

2. **Queue patterns**:
   - Job queue (conversion jobs)
   - Rate limiting
   - Caching layer

3. **Configuration**:
   - Environment-based config
   - Connection retry logic
</action>
<verify>