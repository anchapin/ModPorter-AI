---
phase: "17-08"
plan: "01"
type: execute
wave: 1
depends_on:
  - "17-07"
files_modified:
  - ai-engine/converters/villager_converter.py
  - ai-engine/knowledge/patterns/villager_patterns.py
  - ai-engine/tests/test_villager_conversion.py
autonomous: true
requirements:
  - CONV-17-08
---

<objective>
Implement villager and trade conversion from Java mods to Bedrock, including villager professions, careers, and custom trades.
</objective>

<context>
@ai-engine/converters/potion_converter.py (recently created converter)
@ai-engine/knowledge/patterns/ (existing pattern library)

# Bedrock Villager System
Bedrock uses:
- Villager entities with profession component
- Trade tables in trading/ directory
- .json trade definitions
- Custom merchant recipes

# Java Villager System
Java mods define:
- VillagerProfession registry
- MerchantRecipe lists
- VillagerCareer classes
- Custom trade offers
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VillagerConverter Module</name>
  <files>ai-engine/converters/villager_converter.py</files>
  <read_first>
    - ai-engine/converters/potion_converter.py (for converter pattern)
  </read_first>
  <action>
Create villager/trade converter:

1. **VillagerProfession enum**:
   - NONE, ARMORER, BUTCHER, CARTOGRAPHER, CLERIC, FARMER, FISHERMAN, FLETCHER, LEATHERWORKER, LIBRARIAN, MASON, SHEPHERD, TOOLSMITH, WEAPONSMITH

2. **VillagerConverter class**:
   - convert_villager(java_villager) → villager entity
   - convert_profession(java_prof) → profession component
   - convert_career(java_career) → career definition

3. **Trade conversion**:
   - convert_trade(java_trade) → trade definition
   - convert_merchant_recipe(recipe) → trade JSON
   - convert_price_multiplier() → price adjustments

4. **Trade table generation**:
   - generate_trade_file(profession, trades) → trade.json
   - create_custom_trades(trade_list) → custom trades
</action>
<verify>
<automated>python -c "
from converters.villager_converter import VillagerConverter, VillagerProfession
vc = VillagerConverter()
print('VillagerConverter initialized successfully')
"</automated>
</verify>
<done>Complete VillagerConverter with profession and trade conversion</done>
</task>

<task type="auto">
  <name>Task 2: Create VillagerPatternLibrary</name>
<files>ai-engine/knowledge/patterns/villager_patterns.py</files>
<read_first>
    - ai-engine/knowledge/patterns/potion_patterns.py (for pattern reference)
  </read_first>
<action>
Create villager pattern library:

1. **VillagerPattern dataclass**:
   - java_profession_class: str
   - bedrock_profession_id: str
   - category: str (agriculture/combat/commerce/knowledge)
   - conversion_notes: str

2. **VillagerPatternLibrary class**:
   - patterns: List[VillagerPattern]
   - search_by_java(java_class) → List[VillagerPattern]
   - get_by_category(category) → List[VillagerPattern]

3. **Add 20+ patterns**:
   - **Agriculture**: farmer, fisherman, shepherd, butcher
   - **Combat**: armorsmith, weaponsmith, toolsmith
   - **Commerce**: cartographer, merchant
   - **Knowledge**: librarian, cleric
</action>
<verify>
<automated>python -c "
from knowledge.patterns.villager_patterns import VillagerPatternLibrary
lib = VillagerPatternLibrary()
patterns = lib.search_by_java('Farmer')
print(f'Found {len(patterns)} villager patterns')
"</automated>
</verify>
<done>VillagerPatternLibrary with 20+ patterns</done>
</task>

<task type="auto">
  <name>Task 3: Implement Trade Offer Conversion</name>
<files>ai-engine/converters/villager_converter.py</files>
<read_first>
    - ai-engine/converters/villager_converter.py (created in Task 1)
  </read_first>
  <action>
Add trade offer conversion:

1. **TradeOfferConverter class**:
   - convert_offer(java_offer) → trade offer
   - convert_wants(wants_list) → input items
   - convert_gives(gives_item) → output item
   - convert_trade_level(level) → tier

2. **Trade properties**:
   - convert_max_uses() → max uses
   - convert_experience() → XP reward
   - convert_price_adjustment() → demand multiplier

3. **Custom trades**:
   - convert_custom_trade(java_custom) → custom offer
   - convert_unique_trade() → special trades
</action>
<verify>
<automated>python -c "
from converters.villager_converter import TradeOfferConverter
toc = TradeOfferConverter()
result = toc.convert_offer('emerald', 'diamond_sword')
print('Trade offer conversion works')
"</automated>
</verify>
<done>Complete trade offer conversion</done>
</task>

<task type="auto">
  <name>Task 4: Create Unit Tests</name>
<files>ai-engine/tests/test_villager_conversion.py</files>
  <read_first>
    - ai-engine/tests/test_potion_conversion.py (test pattern)
  </read_first>
<action>
Create unit tests:

1. **TestProfessionConversion** (5 tests)
2. **TestTradeConversion** (5 tests)
3. **TestTradeOfferConversion** (4 tests)
4. **TestVillagerPatterns** (4 tests)
5. **TestIntegration** (2 tests)
</action>
<verify>
<automated>python -m pytest ai-engine/tests/test_villager_conversion.py -v --tb=short 2>&1 | tail -30</automated>
</verify>
<done>20 unit tests covering villager conversion</done>
</task>

</tasks>

<verification>
<automated>python -c "from converters.villager_converter import VillagerConverter, TradeOfferConverter; from knowledge.patterns.villager_patterns import VillagerPatternLibrary; print('All imports OK')"</automated>
</verification>

<success_criteria>
- [ ] VillagerConverter with profession mapping
- [ ] TradeOfferConverter for trade offers
- [ ] VillagerPatternLibrary with 20+ patterns
- [ ] 20 unit tests passing
- [ ] All imports work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/17-08-villager-trade-conversion/17-08-SUMMARY.md`
</output>