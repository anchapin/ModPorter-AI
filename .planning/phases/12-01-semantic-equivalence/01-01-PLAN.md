---
phase: 12-01-semantic-equivalence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - REQ-4.1

# Goal-backward verification (derived during planning, verified after execution)
must_haves:
  truths:
    - Semantic equivalence scores are generated for all conversions
    - Scores categorize correctly: Excellent (90%+), Good (70-89%), Needs Work (<70%)
    - Semantic drift is identified and reported
  artifacts:
    - ai-engine/services/semantic_equivalence.py enhanced with embeddings
    - Integration with conversion pipeline
    - Updated conversion reports include semantic scores
  key_links:
    - SemanticEquivalenceChecker integrated into QAValidatorAgent
    - Scores displayed in conversion reports
---

<objective>
Implement semantic equivalence scoring to measure and track semantic similarity between Java source and Bedrock output.

Purpose: Ensure behavior preservation in conversions by quantifying how semantically similar the output is to the input.
Output: Enhanced semantic_equivalence.py with embeddings, pipeline integration, and report updates.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

[Relevant source files:]
@ai-engine/services/semantic_equivalence.py
@ai-engine/agents/qa_validator.py
@ai-engine/scripts/test_semantic_equivalence.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance SemanticEquivalenceChecker with embedding-based scoring</name>
  <files>ai-engine/services/semantic_equivalence.py</files>
  <action>
Add embedding-based similarity scoring to the SemanticEquivalenceChecker class:
1. Add import for sentence-transformers (or OpenAI embeddings as fallback)
2. Add _compute_embedding_similarity() method that generates code embeddings
3. Enhance EquivalenceResult to include embedding_similarity score
4. Add _identify_semantic_drift() method to categorize differences
5. Implement apply_thresholds() method to categorize scores (90%+/70-89%/<70%)

Avoid: Breaking existing DFG/CFG comparison - maintain backward compatibility.
Why: Existing graph-based comparison provides structural similarity; embeddings add semantic understanding.
  </action>
  <verify>
grep -n "embedding_similarity\|apply_thresholds\|_identify_semantic_drift" ai-engine/services/semantic_equivalence.py
  </verify>
  <done>
SemanticEquivalenceChecker has embedding_similarity method and threshold categorization
</done>
</task>

<task type="auto">
  <name>Task 2: Add JavaScript/Bedrock parsing support</name>
  <files>ai-engine/services/semantic_equivalence.py</files>
  <action>
Extend DataFlowAnalyzer and ControlFlowAnalyzer to handle JavaScript code:
1. Add analyze_javascript() method to DataFlowAnalyzer
2. Add CFG construction for JavaScript using regex-based statement extraction
3. Handle JS-specific patterns: arrow functions, async/await, Promises
4. Map Java method calls to JavaScript function calls for comparison

Avoid: Overcomplicating JS parsing - use regex-based extraction for comparison purposes.
Why: Bedrock scripts are relatively simple; full AST not required for equivalence checking.
  </action>
  <verify>
grep -n "analyze_javascript\|def analyze_js" ai-engine/services/semantic_equivalence.py
  </verify>
  <done>
DataFlowAnalyzer can process JavaScript source code
</done>
</task>

<task type="auto">
  <name>Task 3: Integrate semantic scoring into QAValidatorAgent</name>
  <files>ai-engine/agents/qa_validator.py</files>
  <action>
Add semantic equivalence checking to the QA validation flow:
1. Import SemanticEquivalenceChecker from semantic_equivalence module
2. Add semantic_score validation step in QAValidatorAgent validation flow
3. Pass Java source and generated Bedrock code to checker
4. Include semantic score in validation results
5. Add semantic score threshold checking (fail if <70%)

Avoid: Adding significant latency - use caching for repeated comparisons.
Why: QAValidatorAgent is the natural integration point for post-conversion validation.
  </action>
<verify>
grep -n "semantic_equivalence\|SemanticEquivalence" ai-engine/agents/qa_validator.py
  </verify>
  <done>
QAValidatorAgent performs semantic equivalence scoring as part of validation
</done>
</task>

<task type="auto">
  <name>Task 4: Add semantic scores to conversion reports</name>
  <files>ai-engine/services/validation_reporting.py, ai-engine/crew/conversion_crew.py</files>
  <action>
Include semantic equivalence scores in conversion output reports:
1. Update validation report schema to include semantic_score field
2. Add semantic score display in report generation
3. Include drift identification in report
4. Add semantic score summary in conversion results

Avoid: Breaking existing report format - add as new section.
Why: Users need to understand conversion quality through semantic equivalence.
  </action>
  <verify>
grep -n "semantic.*score\|equivalence.*score" ai-engine/services/validation_reporting.py
  </verify>
  <done>
Conversion reports include semantic equivalence scoring section
</done>
</task>

<task type="auto">
  <name>Task 5: Write tests for semantic equivalence scoring</name>
  <files>ai-engine/tests/test_semantic_equivalence.py</files>
  <action>
Create comprehensive tests for the enhanced semantic equivalence system:
1. Test embedding similarity computation
2. Test threshold categorization (90%+/70-89%/<70%)
3. Test JavaScript parsing and comparison
4. Test semantic drift identification
5. Test integration with QAValidatorAgent
6. Test end-to-end with sample Java and Bedrock code

Avoid: Testing existing DFG/CFG functionality that already works.
Why: Focus on new embedding and threshold features.
  </action>
  <verify>
python -m pytest ai-engine/tests/test_semantic_equivalence.py -v --tb=short 2>&1 | tail -30
  </verify>
  <done>
All semantic equivalence tests pass with >80% coverage on new features
</done>
</task>

</tasks>
