---
phase: 15-06-cross-reference-linking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-engine/knowledge/schema.py
  - ai-engine/knowledge/__init__.py
  - ai-engine/search/hybrid_search_engine.py
  - backend/src/api/routes/knowledge.py
  - backend/tests/unit/test_cross_reference_api.py
autonomous: true
requirements:
  - RAG-6.1
  - RAG-6.2

must_haves:
  truths:
    - "User can discover related concepts when viewing a document"
    - "System automatically detects semantic relationships between chunks"
    - "Concept graph persists relationships for future queries"
  artifacts:
    - path: "ai-engine/knowledge/schema.py"
      provides: "Concept graph database models"
      contains: "ConceptRelationship, ConceptNode models"
    - path: "ai-engine/knowledge/cross_reference.py"
      provides: "Cross-reference detection"
      contains: "CrossReferenceDetector class"
    - path: "backend/src/api/routes/knowledge.py"
      provides: "Related documents API"
      contains: "GET /knowledge/related endpoint"
  key_links:
    - from: "hybrid_search_engine.py"
      to: "knowledge/schema.py"
      via: "ConceptRelationship queries"
---

<objective>
Implement cross-reference linking system to connect related concepts across knowledge base.
</objective>

<context>
@.planning/phases/15-05-user-correction-learning/15-05-02-SUMMARY.md
@ai-engine/knowledge/__init__.py
@ai-engine/search/hybrid_search_engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database Schema for Concept Graph</name>
  <files>ai-engine/knowledge/schema.py</files>
  <action>
Create database models for concept graph relationships:
- `ConceptNode`: id, name, type (class/method/event/property/concept), document_id, created_at
- `ConceptRelationship`: id, source_node_id, target_node_id, relationship_type (extends/implements/calls/uses/related_to), confidence, created_at

Use SQLAlchemy. Ensure compatibility with existing database setup.
  </action>
  <verify>
python3 -c "from knowledge.schema import ConceptNode, ConceptRelationship; print('Import successful')"
  </verify>
  <done>ConceptNode and ConceptRelationship models imported successfully</done>
</task>

<task type="auto">
  <name>Task 2: Cross-Reference Detection Module</name>
  <files>ai-engine/knowledge/cross_reference.py</files>
  <action>
Implement CrossReferenceDetector class with:
- `detect_relationships(chunk)`: Analyzes a chunk and finds related concepts using:
  - Keyword extraction (class names, method calls, imports)
  - Semantic similarity via embeddings
  - Pattern matching (extends, implements, uses patterns)
- `find_related_chunks(chunk_id, limit=5)`: Returns related chunks from database
- `build_concept_graph()`: Batch processes all chunks to build graph

Store relationships in ConceptRelationship table with confidence scores (0.0-1.0).
  </action>
  <verify>
python3 -c "from knowledge.cross_reference import CrossReferenceDetector; print('Import successful')"
  </verify>
  <done>CrossReferenceDetector class imported and functional</done>
</task>

<task type="auto">
  <name>Task 3: Related Documents API Endpoint</name>
  <files>backend/src/api/routes/knowledge.py</files>
  <action>
Add to backend API:
- `GET /knowledge/chunks/{chunk_id}/related` - Returns related chunks
- Query parameters: limit (default 5), relationship_type (optional filter)

Response format:
```json
{
  "chunk_id": "uuid",
  "related": [
    {"chunk_id": "uuid", "title": "...", "relationship_type": "related_to", "confidence": 0.85}
  ]
}
```
  </action>
  <verify>
cd backend && python3 -m pytest tests/unit/test_cross_reference_api.py -v
  </verify>
  <done>API endpoint returns related chunks with confidence scores</done>
</task>

<task type="auto">
  <name>Task 4: Integration with HybridSearchEngine</name>
  <files>ai-engine/search/hybrid_search_engine.py</files>
  <action>
Update HybridSearchEngine to include related concepts in search results:
- Add `include_related: bool = True` parameter to search()
- When enabled, fetch related chunks for each result
- Include related concept IDs in SearchResult metadata

This enables users to discover more relevant content through concept connections.
  </action>
  <verify>
python3 -c "from search import HybridSearchEngine; import inspect; sig = inspect.signature(HybridSearchEngine.search); print('include_related' in sig.parameters)"
  </verify>
  <done>HybridSearchEngine includes related concepts in results</done>
</task>

</tasks>

<verification>
- All imports work correctly
- Database schema created and migrations applied
- API endpoint responds with related chunks
- Unit tests pass
</verification>

<success_criteria>
Phase 15-06-01 complete when:
- [ ] ConceptNode and ConceptRelationship models exist
- [ ] CrossReferenceDetector can detect and store relationships
- [ ] API returns related documents with confidence scores
- [ ] HybridSearchEngine integrates with cross-reference system
- [ ] Unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-06-cross-reference-linking/15-06-01-SUMMARY.md`
</output>
