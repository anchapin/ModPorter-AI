---
phase: 16-01-qa-context-orchestration
plan: "02"
type: execute
wave: 2
depends_on: ["16-01-01"]
files_modified: []
autonomous: true
requirements: ["QA-01"]
user_setup: []

must_haves:
  truths:
    - "QAOrchestrator coordinates 4-agent pipeline sequentially"
    - "Each agent execution wrapped with 5-min timeout"
    - "Circuit breaker handles failed agents gracefully"
    - "Context passes between agents with merged validation_results"
  artifacts:
    - path: "ai-engine/src/qa/orchestrator.py"
      provides: "QAOrchestrator class"
      exports: ["QAOrchestrator"]
  key_links:
    - from: "orchestrator.py"
      to: "context.py"
      via: "QAContext parameter and return"
    - from: "orchestrator.py"
      to: "validators.py"
      via: "validate_agent_output after each agent"
    - from: "orchestrator.py"
      to: "utils.error_recovery"
      via: "CircuitBreaker wrapper"
---

<objective>
Create QAOrchestrator class that coordinates 4-agent pipeline with sequential execution, timeout, and circuit breaker.

Purpose: Core orchestration logic for multi-agent QA pipeline
Output: QAOrchestrator with run_qa_pipeline method
</objective>

<execution_context>
@/home/alex/.agents/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/16-01-qa-context-orchestration/16-01-01-SUMMARY.md
@.planning/phases/16-01-qa-context-orchestration/16-01-RESEARCH.md

Reference from utils/error_recovery.py:87:
```python
from utils.error_recovery import CircuitBreaker, CircuitBreakerOpenError

breaker = CircuitBreaker(name="translator_agent", fail_max=3, reset_timeout=300)
try:
    result = breaker.execute(agent.run, context)
except CircuitBreakerOpenError:
    logger.error("Agent circuit open, skipping")
```

Reference from search/rag_pipeline.py:289 (RAGPipeline pattern):
- Stage-based execution with context passing
- Sequential execution of stages
- Results merged into context
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create QAOrchestrator class</name>
  <files>ai-engine/src/qa/orchestrator.py</files>
  <action>
Create QAOrchestrator class with:

1. __init__:
   - self.timeout_seconds = 300 (5 min default)
   - self.agents = ["translator", "reviewer", "tester", "semantic_checker"]
   - Initialize CircuitBreaker for each agent (fail_max=3, reset_timeout=300)
   - Use structlog for logging

2. run_qa_pipeline(context: QAContext) -> QAContext:
   - For each agent in self.agents:
     - Set context.current_agent = agent_name
     - Execute agent with timeout and circuit breaker
     - Validate output with validate_agent_output
     - Merge result into context.validation_results
   - Return updated context

3. run_agent method:
   - Wraps agent execution with asyncio.wait_for (timeout)
   - Wraps with CircuitBreaker.execute
   - Returns AgentOutput

4. _execute_agent_placeholder method:
   - Placeholder that simulates agent execution
   - Returns mock AgentOutput with success=True
   - Will be replaced by actual agent implementations in Phase 16-02 to 16-05

Use asyncio for timeout, CircuitBreaker from utils.error_recovery.
Import: asyncio, structlog, logging, Dict, Any from typing.
</action>
  <verify>
    <automated>python -c "
from qa.orchestrator import QAOrchestrator
from qa.context import QAContext
from pathlib import Path

orch = QAOrchestrator()
ctx = QAContext(job_id='test', job_dir=Path('.'), source_java_path=Path('.'), output_bedrock_path=Path('.'))
result = orch.run_qa_pipeline(ctx)
print('OK - agents run:', len(result.validation_results))
"</automated>
  </verify>
  <done>QAOrchestrator runs pipeline and returns context with validation_results</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add timeout and circuit breaker tests</name>
  <files>ai-engine/tests/test_qa_orchestrator.py</files>
  <behavior>
- Test 1: run_qa_pipeline executes all 4 agents
- Test 2: context.current_agent updates after each agent
- Test 3: validation_results contains all agent results
- Test 4: Circuit breaker opens after fail_max failures
- Test 5: Timeout raises asyncio.TimeoutError
  </behavior>
  <action>
Write pytest tests:
- test_orchestrator_runs_all_agents: Verify 4 agents executed
- test_context_passes_between_agents: Verify current_agent updates
- test_validation_results_merged: Verify all results in context
- test_circuit_breaker_opens: Mock failure, verify circuit opens
- test_timeout_enforced: Mock slow agent, verify timeout

Use pytest.mark.asyncio for async tests, unittest.mock for mocking.
</action>
  <verify>
    <automated>pytest ai-engine/tests/test_qa_orchestrator.py -x -v</automated>
  </verify>
  <done>All orchestrator tests pass</done>
</task>

</tasks>

<verification>
- QAOrchestrator executes all 4 agents sequentially
- Timeout enforced at 5 minutes per agent
- Circuit breaker handles failures gracefully
- Context passes between agents with validation_results merged
</verification>

<success_criteria>
QAOrchestrator coordinates 4-agent pipeline with timeout and circuit breaker
</success_criteria>

<output>
After completion, create `.planning/phases/16-01-qa-context-orchestration/16-01-02-SUMMARY.md`
</output>