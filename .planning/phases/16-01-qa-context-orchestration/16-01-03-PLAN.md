---
phase: 16-01-qa-context-orchestration
plan: "03"
type: execute
wave: 3
depends_on: ["16-01-02"]
files_modified: []
autonomous: true
requirements: ["QA-01"]
user_setup: []

must_haves:
  truths:
    - "QA hook can be triggered after packaging completes"
    - "Hook integrates with existing pipeline configuration"
    - "QA results attachable to conversion output"
  artifacts:
    - path: "ai-engine/src/qa/hooks.py"
      provides: "Post-conversion QA integration"
      exports: ["QAIntegrationHook", "run_post_conversion_qa"]
  key_links:
    - from: "hooks.py"
      to: "orchestrator.py"
      via: "QAOrchestrator.run_qa_pipeline"
    - from: "agents/packaging_agent.py"
      to: "hooks.py"
      via: "Optional QA integration (configurable)"
---

<objective>
Create post-conversion QA integration hook to trigger QA pipeline after PackagingAgent completes.

Purpose: Integrate QA pipeline into main conversion workflow
Output: QAIntegrationHook that can be called from packaging
</objective>

<execution_context>
@/home/alex/.agents/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/16-01-qa-context-orchestration/16-01-02-SUMMARY.md
@.planning/phases/16-01-qa-context-orchestration/16-01-RESEARCH.md

Reference from research (Hook Integration Pattern):
```python
from agents.qa_orchestrator import QAOrchestrator

def build_mcaddon(self, build_data):
    # ... existing packaging logic ...
    
    # Post-packaging QA hook
    if settings.QA_ENABLED:
        qa_orchestrator = QAOrchestrator()
        qa_result = qa_orchestrator.run_qa_pipeline(job_id, temp_dir)
        # Attach QA result to output
```

Reference from agents/packaging_agent.py:
- PackagingAgent has build_mcaddon method
- Can be extended to call QA hook
- Need to make QA optional via config
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create QA integration hook</name>
  <files>ai-engine/src/qa/hooks.py</files>
  <action>
Create QAIntegrationHook class with:

1. __init__:
   - Optional enabled flag (default True for dev, False for prod until stable)
   - QAOrchestrator instance

2. run_post_conversion_qa(job_dir: Path) -> Dict[str, Any]:
   - Scan job_dir for source Java files and output Bedrock files
   - Create QAContext with discovered paths
   - Run orchestrator.run_qa_pipeline(context)
   - Return result with summary, scores, issues

3. create_qa_context_from_job_dir(job_dir: Path) -> QAContext:
   - Discover source Java files (*.java in job_dir)
   - Discover output Bedrock files (behavior_pack/, resource_pack/)
   - Build metadata about conversion
   - Return QAContext

Add pydantic Settings for QA_ENABLED flag.
Use Path, glob for file discovery.
</action>
  <verify>
    <automated>python -c "
from qa.hooks import QAIntegrationHook, run_post_conversion_qa
print('OK - hooks module imports work')
"</automated>
  </verify>
  <done>QAIntegrationHook can be instantiated and imported</done>
</task>

<task type="auto">
  <name>Task 2: Create integration test</name>
  <files>ai-engine/tests/test_qa_integration.py</files>
  <action>
Write pytest integration tests:

- test_hook_discovers_files: Create temp dirs with mock files, verify discovery
- test_hook_creates_qa_context: Verify context has correct paths
- test_hook_returns_summary: Verify run_post_conversion_qa returns result dict

Use pytest tmp_path for temp directories.
Mock orchestrator.run_qa_pipeline to avoid running full pipeline in test.
</action>
  <verify>
    <automated>pytest ai-engine/tests/test_qa_integration.py -x -v</automated>
  </verify>
  <done>Integration tests pass</done>
</task>

</tasks>

<verification>
- QAIntegrationHook discovers source and output files
- QAContext created with correct paths
- run_post_conversion_qa returns summary dict
- Integration with packaging workflow is configurable
</verification>

<success_criteria>
Post-conversion QA hook integrated and testable
</success_criteria>

<output>
After completion, create `.planning/phases/16-01-qa-context-orchestration/16-01-03-SUMMARY.md`
</output>