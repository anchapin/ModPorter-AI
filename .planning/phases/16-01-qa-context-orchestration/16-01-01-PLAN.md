---
phase: 16-01-qa-context-orchestration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: ["QA-01"]
user_setup: []

must_haves:
  truths:
    - "QAContext dataclass exists with all required fields"
    - "AgentOutput schema validates each agent's output"
    - "Schema validation fails fast on malformed output"
  artifacts:
    - path: "ai-engine/src/qa/__init__.py"
      provides: "QA module exports"
    - path: "ai-engine/src/qa/context.py"
      provides: "QAContext dataclass"
      exports: ["QAContext"]
    - path: "ai-engine/src/qa/validators.py"
      provides: "AgentOutput schema validation"
      exports: ["AgentOutput", "validate_agent_output"]
  key_links:
    - from: "context.py"
      to: "validators.py"
      via: "validation_results field uses AgentOutput"
---

<objective>
Create QA context dataclass and output schema validators for the multi-agent QA system.

Purpose: Establish type-safe context passing and output validation between QA agents
Output: QAContext dataclass and AgentOutput validator
</objective>

<execution_context>
@/home/alex/.agents/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/16-01-qa-context-orchestration/16-01-RESEARCH.md

From research (Standard Stack):
- Use pydantic >=2.0.0 for dataclass
- Use Path from pathlib for file paths
- Use datetime with Field(default_factory=datetime.now)

From research (Architecture Pattern 1):
```python
from pydantic import BaseModel, Field
from typing import Optional, Dict, Any
from datetime import datetime
from pathlib import Path

class QAContext(BaseModel):
    job_id: str
    job_dir: Path
    source_java_path: Path
    output_bedrock_path: Path
    metadata: Dict[str, Any] = {}
    validation_results: Dict[str, Any] = {}
    created_at: datetime = Field(default_factory=datetime.now)
    current_agent: Optional[str] = None
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create QA module structure</name>
  <files>ai-engine/src/qa/__init__.py</files>
  <action>
Create ai-engine/src/qa/ directory with __init__.py that exports:
- QAContext from context
- AgentOutput, validate_agent_output from validators
</action>
  <verify>
    <automated>python -c "from qa import QAContext, AgentOutput, validate_agent_output; print('OK')"</automated>
  </verify>
  <done>QA module imports work correctly</done>
</task>

<task type="auto">
  <name>Task 2: Create QAContext dataclass</name>
  <files>ai-engine/src/qa/context.py</files>
  <action>
Create QAContext as pydantic BaseModel with fields:
- job_id: str
- job_dir: Path
- source_java_path: Path  
- output_bedrock_path: Path
- metadata: Dict[str, Any] = {}
- validation_results: Dict[str, Any] = {}
- created_at: datetime = Field(default_factory=datetime.now)
- current_agent: Optional[str] = None

Use pydantic Field for default_factory. Use Optional from typing.
</action>
  <verify>
    <automated>python -c "from qa.context import QAContext; from pathlib import Path; c = QAContext(job_id='test', job_dir=Path('.'), source_java_path=Path('.'), output_bedrock_path=Path('.')); print('OK:', c.job_id)"</automated>
  </verify>
  <done>QAContext can be instantiated with required fields</done>
</task>

<task type="auto">
  <name>Task 3: Create AgentOutput validators</name>
  <files>ai-engine/src/qa/validators.py</files>
  <action>
Create AgentOutput pydantic BaseModel with:
- agent_name: str
- success: bool
- result: Dict[str, Any]
- errors: List[str] = []
- execution_time_ms: int

Create validate_agent_output function that:
- Takes Dict input
- Returns AgentOutput instance
- Raises ValidationError with clear message on failure

Import List from typing, ValidationError from pydantic.
</action>
  <verify>
    <automated>python -c "from qa.validators import AgentOutput, validate_agent_output; out = validate_agent_output({'agent_name': 'test', 'success': True, 'result': {}, 'execution_time_ms': 100}); print('OK:', out.agent_name)"</automated>
  </verify>
  <done>validate_agent_output returns valid AgentOutput</done>
</task>

</tasks>

<verification>
- QA module imports work: `from qa import QAContext, AgentOutput, validate_agent_output`
- QAContext instantiation works with required fields
- validate_agent_output properly validates and returns AgentOutput
- Invalid input raises ValidationError
</verification>

<success_criteria>
QAContext and AgentOutput schemas implemented with pydantic validation
</success_criteria>

<output>
After completion, create `.planning/phases/16-01-qa-context-orchestration/16-01-01-SUMMARY.md`
</output>