---
phase: 15-04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-engine/search/context_manager.py
  - ai-engine/search/query_complexity_analyzer.py
  - ai-engine/indexing/chunk_prioritizer.py
  - backend/tests/unit/test_context_optimization.py
autonomous: true
requirements:
  - RAG-4.1
  - RAG-4.2

must_haves:
  truths:
    - "Query complexity is classified before search (simple/standard/complex)"
    - "Context window size dynamically adjusts based on query complexity"
    - "Relevant chunks are prioritized over less relevant ones"
    - "Long documents are compressed without losing key information"
    - "Multi-turn conversations maintain context across turns"
  artifacts:
    - path: "ai-engine/search/context_manager.py"
      provides: "Context window sizing and multi-turn management"
      exports: ["ContextManager", "DynamicContextSizer"]
    - path: "ai-engine/search/query_complexity_analyzer.py"
      provides: "Query complexity classification"
      exports: ["QueryComplexityAnalyzer", "ComplexityLevel"]
    - path: "ai-engine/indexing/chunk_prioritizer.py"
      provides: "Relevancy-based chunk prioritization"
      exports: ["ChunkPrioritizer", "RelevanceScore"]
  key_links:
    - from: "ai-engine/search/query_complexity_analyzer.py"
      to: "ai-engine/search/hybrid_search_engine.py"
      via: "complexity passed to search"
    - from: "ai-engine/search/context_manager.py"
      to: "ai-engine/indexing/chunk_prioritizer.py"
      via: "relevance scores used for prioritization"
---

<objective>
Implement context window optimization for the RAG system, enabling dynamic context sizing based on query complexity and relevancy-based chunk prioritization.
</objective>

<context>
@.planning/phases/15-04-context-optimization/15-04-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Key Interfaces from Prior Phases

From ai-engine/search/hybrid_search_engine.py:
```python
class HybridSearchEngine:
    async def search(self, query: str, top_k: int = 5, filters: dict = None) -> SearchResult:
        """Perform hybrid search with semantic and keyword matching."""
```

From ai-engine/indexing/chunking_strategies.py:
```python
class ChunkingStrategy(ABC):
    def chunk(self, content: str, chunk_size: int = 512, overlap: int = 50) -> list[Chunk]:
        """Split content into chunks."""
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Query Complexity Analyzer</name>
  <files>ai-engine/search/query_complexity_analyzer.py</files>
  <read_first>
    - ai-engine/search/hybrid_search_engine.py (existing search interface)
  </read_first>
  <action>
Create QueryComplexityAnalyzer class that classifies queries into complexity levels:

1. **ComplexityLevel enum**: SIMPLE, STANDARD, COMPLEX
2. **QueryComplexityAnalyzer class**:
   - `analyze(query: str) -> ComplexityLevel`
   - Classification criteria:
     - SIMPLE: Single concept, <5 tokens, no technical terms (e.g., "item definition")
     - STANDARD: Multiple concepts, 5-15 tokens, basic technical terms (e.g., "how to create block")
     - COMPLEX: Multi-part question, >15 tokens, multiple technical terms, conditional logic (e.g., "convert entity with AI behavior")
   - Use heuristics: token count, keyword matching, question words, technical term detection
   - Return complexity level with confidence score

3. **Integration**: Export ComplexityLevel enum for use by search engine
  </action>
  <acceptance_criteria>
- [ ] ComplexityLevel enum has SIMPLE, STANDARD, COMPLEX values
- [ ] QueryComplexityAnalyzer.analyze("item") returns ComplexityLevel.SIMPLE
- [ ] QueryComplexityAnalyzer.analyze("how to create custom block") returns ComplexityLevel.STANDARD
- [ ] QueryComplexityAnalyzer.analyze() handles complex multi-part queries
- [ ] Unit tests verify classification for each complexity level
  </acceptance_criteria>
  <verify>
    <automated>cd /home/alex/Projects/portkit && python -c "
from ai_engine.search.query_complexity_analyzer import QueryComplexityAnalyzer, ComplexityLevel
analyzer = QueryComplexityAnalyzer()
assert analyzer.analyze('item') == ComplexityLevel.SIMPLE
assert analyzer.analyze('how to create custom block') == ComplexityLevel.STANDARD
print('Query complexity analyzer tests passed')
"</automated>
  </verify>
  <done>Query complexity analyzer classifies queries into SIMPLE/STANDARD/COMPLEX with confidence scores</done>
</task>

<task type="auto">
  <name>Task 2: Implement Dynamic Context Sizer</name>
  <files>ai-engine/search/context_manager.py</files>
  <read_first>
    - ai-engine/search/query_complexity_analyzer.py (created in Task 1)
    - ai-engine/indexing/chunking_strategies.py (existing chunking)
  </read_first>
  <action>
Create DynamicContextSizer class that adjusts context window based on complexity:

1. **ContextConfig dataclass**:
   - max_tokens: int
   - overlap_tokens: int
   - min_chunks: int
   - max_chunks: int

2. **DynamicContextSizer class**:
   - `get_config(complexity: ComplexityLevel) -> ContextConfig`
   - Configuration by complexity:
     - SIMPLE: max_tokens=1000, min_chunks=2, max_chunks=5
     - STANDARD: max_tokens=2000, min_chunks=3, max_chunks=10
     - COMPLEX: max_tokens=4000, min_chunks=5, max_chunks=20
   - `calculate_chunks(query: str, available_chunks: list, config: ContextConfig) -> list`
   - Truncate or expand chunk selection based on config

3. **ContextManager class** (for multi-turn):
   - `add_turn(user_query: str, assistant_response: str)`
   - `get_context_window() -> list[Turn]`
   - `clear()` - reset conversation
   - Keep last 3-5 turns based on token budget
  </action>
  <acceptance_criteria>
- [ ] DynamicContextSizer returns different ContextConfig per complexity level
- [ ] SIMPLE queries get smaller context window (max 5 chunks)
- [ ] COMPLEX queries get larger context window (max 20 chunks)
- [ ] ContextManager maintains conversation history
- [ ] ContextManager respects token budget when trimming history
  </acceptance_criteria>
  <verify>
    <automated>cd /home/alex/Projects/portkit && python -c "
from ai_engine.search.context_manager import DynamicContextSizer, ContextManager, ContextConfig
from ai_engine.search.query_complexity_analyzer import ComplexityLevel

sizer = DynamicContextSizer()
simple_cfg = sizer.get_config(ComplexityLevel.SIMPLE)
complex_cfg = sizer.get_config(ComplexityLevel.COMPLEX)
assert simple_cfg.max_chunks == 5
assert complex_cfg.max_chunks == 20
print('Dynamic context sizer tests passed')
"</automated>
  </verify>
  <done>Dynamic context sizer adjusts window size based on query complexity (5/10/20 chunks)</done>
</task>

<task type="auto">
  <name>Task 3: Implement Chunk Prioritizer</name>
  <files>ai-engine/indexing/chunk_prioritizer.py</files>
  <read_first>
    - ai-engine/indexing/chunking_strategies.py (existing chunk structure)
    - ai-engine/search/reranking_engine.py (existing re-ranking)
  </read_first>
  <action>
Create ChunkPrioritizer class for relevancy-based chunk ordering:

1. **RelevanceScore dataclass**:
   - chunk_id: str
   - score: float (0-1)
   - reasons: list[str]

2. **ChunkPrioritizer class**:
   - `prioritize(query: str, chunks: list[Chunk]) -> list[tuple[Chunk, RelevanceScore]]`
   - Scoring factors:
     - Keyword overlap (exact matches)
     - Semantic similarity (embedding cosine)
     - Position weight (earlier chunks in document)
     - Heading context (chunks near headings)
   - Combine scores with weighted formula
   - Return sorted chunks with relevance scores

3. **Integration**: Works with existing hybrid_search_engine.py
  </action>
  <acceptance_criteria>
- [ ] ChunkPrioritizer.prioritize() returns chunks sorted by relevance
- [ ] Higher relevance scores for chunks with query keywords
- [ ] Position weighting applied (earlier chunks preferred)
- [ ] Heading context increases relevance score
- [ ] Returns RelevanceScore with reasons for each chunk
  </acceptance_criteria>
  <verify>
    <automated>cd /home/alex/Projects/portkit && python -c "
from ai_engine.indexing.chunk_prioritizer import ChunkPrioritizer
# Basic test - verify class can be instantiated and method exists
prioritizer = ChunkPrioritizer()
assert hasattr(prioritizer, 'prioritize')
print('Chunk prioritizer tests passed')
"</automated>
  </verify>
  <done>Chunk prioritizer ranks chunks by relevance with explainable scores</done>
</task>

</tasks>

<verification>
- [ ] All 3 tasks complete with passing tests
- [ ] Integration test: query complexity → context size → chunk prioritization works end-to-end
- [ ] Multi-turn context management preserves conversation history
- [ ] Backward compatible with existing search API
</verification>

<success_criteria>
Dynamic context window optimization implemented:
- Query complexity analyzer classifies queries (SIMPLE/STANDARD/COMPLEX)
- Dynamic context sizer adjusts window size (5/10/20 chunks)
- Chunk prioritizer ranks results by relevance
- Multi-turn conversations maintain context
- Unit tests verify each component
</success_criteria>

<output>
After completion, create `.planning/phases/15-04-context-optimization/15-04-01-SUMMARY.md`
</output>
