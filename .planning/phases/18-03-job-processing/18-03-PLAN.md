---
phase: "18-03"
plan: "01"
type: execute
wave: 1
depends_on:
  - "18-02"
files_modified:
  - backend/src/worker/conversion_worker.py
  - backend/src/services/job_manager.py
  - backend/src/api/jobs.py
autonomous: true
requirements:
  - INF-18-03
---

<objective>
Implement background job processing system for handling conversion jobs asynchronously with Redis queue.
</objective>

<context>
@backend/src/core/redis.py (from 18-01)
@backend/src/api/upload.py (from 18-02)

# Required Features
- Job queue with Redis
- Background worker processing
- Job status tracking
- Webhook notifications
</context>

<tasks>

<task type="auto">
  <name>Task 1: Job Manager Service</name>
  <files>backend/src/services/job_manager.py</files>
<read_first>
    - backend/src/core/redis.py
  </read_first>
<action>
Create job management service:

1. **Job creation**:
   - create_job(user_id, file_path, options) → job_id
   - Job states: pending, processing, completed, failed

2. **Job tracking**:
   - get_job(job_id) → Job status
   - list_jobs(user_id) → User's jobs
   - update_progress(job_id, progress)

3. **Job options**:
   - Conversion mode (simple/standard/complex)
   - Target version (1.19/1.20/1.21)
   - Output format (mcaddon/zip)
</action>
<verify>
<automated>python3 -c "import sys; sys.path.insert(0, 'backend/src'); from services.job_manager import JobManager; print('JobManager OK')"</automated>
</verify>
<done>Job manager service complete</done>
</task>

<task type="auto">
  <name>Task 2: Conversion Worker</name>
<files>backend/src/worker/conversion_worker.py</files>
<action>
Create background worker:

1. **Worker loop**:
   - Poll Redis queue for new jobs
   - Process job with ai-engine
   - Update status on completion

2. **Processing steps**:
   - Download file from storage
   - Run conversion pipeline
   - Package results
   - Upload to storage
   - Notify via webhook

3. **Error handling**:
   - Retry logic with backoff
   - Dead letter queue for failures
   - Detailed error logging
</action>
<verify>
<automated>python3 -c "import sys; sys.path.insert(0, 'backend/src'); from worker.conversion_worker import ConversionWorker; print('Worker OK')"</automated>
</verify>
<done>Conversion worker complete</done>
</task>

<task type="auto">
  <name>Task 3: Jobs API</name>
  <files>backend/src/api/jobs.py</files>
<action>
Create jobs endpoints:

1. **Endpoints**:
   - GET /api/v1/jobs - List jobs
   - GET /api/v1/jobs/{job_id} - Get job status
   - DELETE /api/v1/jobs/{job_id} - Cancel job

2. **Status responses**:
   - State, progress percentage
   - Current step description
   - Result/download URL when complete

3. **Webhooks**:
   - Register webhook URL per job
   - POST on job completion
</action>
<verify>
<automated>python3 -c "import sys; sys.path.insert(0, 'backend/src'); from api import jobs; print('Jobs API OK')"</automated>
</verify>
<done>Jobs API complete</done>
</task>

<task type="auto">
  <name>Task 4: Unit Tests</name>
<files>backend/tests/test_jobs.py</files>
<action>
Create job tests:

1. **Job Manager tests** (5 tests)
2. **Worker tests** (4 tests)
3. **API tests** (4 tests)
</action>
<verify>
<automated>python3 -m pytest backend/tests/test_jobs.py -v --tb=short 2>&1 | tail -20</automated>
</verify>
<done>13 unit tests passing</done>
</task>

</tasks>

<verification>
<automated>python3 -c "from services.job_manager import JobManager; from worker.conversion_worker import ConversionWorker; from api import jobs; print('All imports OK')"</automated>

<success_criteria>
- [ ] JobManager with job tracking
- [ ] ConversionWorker for background processing
- [ ] Jobs API for status endpoints
- [ ] 13 unit tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/18-03-job-processing/18-03-SUMMARY.md`
</output>