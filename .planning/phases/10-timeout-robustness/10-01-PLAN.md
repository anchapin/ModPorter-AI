---
phase: 10-timeout-robustness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-engine/utils/timeout_manager.py
  - ai-engine/config/timeouts.yaml
  - ai-engine/agents/base_agent.py
  - ai-engine/crew/conversion_crew.py
  - backend/src/api/conversions.py
autonomous: true
requirements:
  - REQ-1.15
user_setup: []

must_haves:
  truths:
    - "All LLM API calls have explicit timeouts"
    - "Agent tasks can be terminated gracefully at deadline"
    - "Pipeline stages timeout independently"
    - "System logs timeout events with context"
  artifacts:
    - path: "ai-engine/utils/timeout_manager.py"
      provides: "Centralized timeout management"
      min_lines: 100
    - path: "ai-engine/config/timeouts.yaml"
      provides: "Timeout configuration"
      contains: "llm_timeout"
    - path: "ai-engine/agents/base_agent.py"
      provides: "Base agent with timeout support"
      contains: "timeout"
  key_links:
    - from: "ai-engine/utils/timeout_manager.py"
      to: "ai-engine/config/timeouts.yaml"
      via: "config loading"
      pattern: "load.*timeout"
    - from: "ai-engine/agents/base_agent.py"
      to: "ai-engine/utils/timeout_manager.py"
      via: "import"
      pattern: "timeout_manager"
---

<objective>
Implement comprehensive timeout and deadline management for all AI conversion operations.

Purpose: Prevent indefinite hangs, ensure predictable behavior, and enable graceful recovery from timeout scenarios.

Output: Timeout management system with configurable per-component timeouts.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Relevant source files:
@ai-engine/utils/
@ai-engine/agents/
@ai-engine/crew/conversion_crew.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create timeout configuration system</name>
  <files>ai-engine/config/timeouts.yaml, ai-engine/utils/timeout_manager.py</files>
  <action>
Create comprehensive timeout configuration in YAML format with the following structure:
- LLM call timeouts: per-provider (openai, anthropic, ollama), per-operation (translate, analyze, validate)
- Agent task deadlines: per agent type, with warning thresholds
- Pipeline stage timeouts: per stage (analysis, conversion, validation, packaging)
- Global defaults with override capability

Then create timeout_manager.py with:
- TimeoutConfig class to load and validate YAML config
- TimeoutContext manager for async operations
- TimeoutExceeded exception with detailed context
- Helper functions for common timeout patterns
  </action>
  <verify>
- YAML file created with all timeout categories
- Python module imports without errors
- TimeoutConfig loads YAML correctly
  </verify>
  <done>
- ai-engine/config/timeouts.yaml exists with full configuration
- ai-engine/utils/timeout_manager.py exists with TimeoutConfig class
  </done>
</task>

<task type="auto">
  <name>Task 2: Add timeout support to base agent</name>
  <files>ai-engine/agents/base_agent.py</files>
  <action>
Update the BaseAgent class to include:
- Timeout configuration in __init__ (load from timeout_manager)
- _execute_with_timeout() method wrapping LLM calls
- Task deadline tracking with warning callbacks
- Graceful termination via TaskTimeout exception
- Timeout event logging with full context

Key considerations:
- Use asyncio.wait_for() for async operations
- Preserve existing functionality
- Add timeout warnings before hard timeout
- Track timeout statistics for monitoring
  </action>
  <verify>
- Import timeout_manager works
- BaseAgent has timeout parameter
- _execute_with_timeout method exists
  </verify>
  <done>
- BaseAgent updated with timeout support
- Existing agent subclasses work without modification
- Timeout events logged properly
  </done>
</task>

<task type="auto">
  <name>Task 3: Add deadline management to conversion crew</name>
  <files>ai-engine/crew/conversion_crew.py</files>
  <action>
Update ConversionCrew to manage deadlines across all pipeline stages:
- Load timeout configuration in crew initialization
- Add stage deadline tracking (analysis, conversion, validation, packaging)
- Implement stage timeout handlers with graceful degradation
- Add overall job deadline with configurable max time (30 min default)
- Track stage progress and time remaining

Key methods to add/modify:
- _run_with_deadline() - wraps crew execution
- _check_stage_deadline() - validates stage can complete
- _handle_stage_timeout() - graceful degradation per stage
- get_progress_with_time() - returns progress with ETA
  </verify>
  <verify>
- ConversionCrew loads timeout config
- Stage timeouts are enforced
- Timeout events logged
  </verify>
  <done>
- ConversionCrew respects pipeline stage timeouts
- Overall job timeout at 30 minutes default
- Graceful handling when stages timeout
  </done>
</task>

<task type="auto">
  <name>Task 4: Add timeout awareness to backend API</name>
  <files>backend/src/api/conversions.py</files>
  <action>
Update the backend conversion API to expose timeout configuration:
- Add timeout configuration to conversion job creation
- Pass timeout settings to AI engine requests
- Add timeout status in job progress
- Handle timeout errors gracefully with proper HTTP responses

Key changes:
- Add timeout field to conversion request schema
- Include timeout in AI engine API call
- Map timeout errors to appropriate HTTP status (504 Gateway Timeout)
- Add timeout info to job status response
  </verify>
  <verify>
- API accepts timeout configuration
- Timeout errors return 504
- Job status includes timeout info
  </verify>
  <done>
- Backend properly configures and handles timeouts
- User can see timeout status in API responses
- Proper error handling for timeout scenarios
  </done>
</task>

</tasks>

<verification>
- [ ] ai-engine/config/timeouts.yaml created with all timeout categories
- [ ] ai-engine/utils/timeout_manager.py created and importable
- [ ] ai-engine/agents/base_agent.py updated with timeout support
- [ ] ai-engine/crew/conversion_crew.py updated with deadline management
- [ ] backend/src/api/conversions.py updated with timeout awareness
- [ ] All Python files pass syntax check (python -m py_compile)
- [ ] Existing tests still pass
</verification>

<success_criteria>
- All timeout configuration is externalized in YAML
- LLM calls have explicit timeouts
- Agent tasks respect deadlines
- Pipeline stages timeout independently
- Timeout events are logged with full context
- Backend API properly handles timeout scenarios
</success_criteria>

<output>
After completion, create `.planning/phases/10-timeout-robustness/10-01-SUMMARY.md`
</output>
