---
phase: 10-timeout-robustness
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-engine/utils/degradation_manager.py
  - ai-engine/utils/partial_converter.py
  - ai-engine/config/degradation_config.yaml
  - ai-engine/agents/base_agent.py
  - ai-engine/crew/conversion_crew.py
autonomous: true
requirements:
  - REQ-1.15
user_setup: []

must_haves:
  truths:
    - "Partial conversions produce usable output when sub-components fail"
    - "Fallback strategies trigger when primary methods fail"
    - "Degraded mode produces valid .mcaddon files"
    - "Users receive clear indication of result completeness"
  artifacts:
    - path: "ai-engine/utils/degradation_manager.py"
      provides: "Centralized degradation management"
      min_lines: 100
    - path: "ai-engine/utils/partial_converter.py"
      provides: "Partial conversion engine"
      min_lines: 80
    - path: "ai-engine/config/degradation_config.yaml"
      provides: "Degradation configuration"
      contains: "fallback_strategies"
---

<objective>
Implement graceful degradation system with partial conversion, fallback strategies, and degraded mode operation.

Purpose: Ensure conversion pipeline produces usable results even when some components fail.
Output: degradation_manager.py, partial_converter.py, degradation_config.yaml, integrated into conversion_crew.py
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference the completed Phase 10-01 for foundation:
@.planning/phases/10-timeout-robustness/10-01-SUMMARY.md
@.planning/phases/10-timeout-robustness/10-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Partial Conversion Engine</name>
  <files>ai-engine/utils/partial_converter.py</files>
  <action>
Create `partial_converter.py` with:
- `PartialConversionResult` dataclass with: components_converted (list), components_failed (list), partial_output (dict), completeness_percentage (float), warnings (list)
- `PartialConverter` class with:
  - `start_conversion()` - Initialize tracking for conversion components
  - `mark_component_success(component, output)` - Record successful component
  - `mark_component_failure(component, error)` - Record failed component  
  - `generate_partial_output()` - Generate valid .mcaddon from successful components
  - `get_completeness_report()` - Return detailed completeness report
- Component types to track: manifest, items, blocks, entities, recipes, scripts, textures, sounds
- Methods to serialize partial results to JSON for reporting
  </action>
  <verify>python -c "from ai_engine.utils.partial_converter import PartialConverter, PartialConversionResult; print('Import OK')"</verify>
  <done>PartialConverter class with success/failure tracking and partial output generation</done>
</task>

<task type="auto">
  <name>Task 2: Create Degradation Manager with Fallback Strategies</name>
  <files>ai-engine/utils/degradation_manager.py</files>
  <action>
Create `degradation_manager.py` with:
- `DegradationLevel` enum: FULL, REDUCED, BASIC, EMERGENCY
- `FallbackStrategy` dataclass with: name, method, conditions, priority
- `DegradationManager` class with:
  - `__init__()` - Load config from degradation_config.yaml
  - `register_fallback(component, strategies)` - Register fallback chain for component
  - `execute_with_fallback(component, primary_method, *args)` - Try primary, fallback on failure
  - `escalate_degradation()` - Move to higher degradation level
  - `get_current_level()` - Return current degradation level
  - `should_degrade(error)` - Determine if error warrants degradation
- Fallback chain logic: Try primary → Try alternatives → Use generic fallback → Record partial failure
- Integration with timeout_manager from Phase 10-01 to trigger degradation on timeout
  </action>
  <verify>python -c "from ai_engine.utils.degradation_manager import DegradationManager, DegradationLevel; print('Import OK')"</verify>
  <done>DegradationManager class with fallback strategy execution and level management</done>
</task>

<task type="auto">
  <name>Task 3: Create Degradation Configuration and Integrate</name>
  <files>ai-engine/config/degradation_config.yaml, ai-engine/crew/conversion_crew.py</files>
  <action>
1. Create `degradation_config.yaml` with:
   - `degradation_levels`: FULL (all validations), REDUCED (skip semantic), BASIC (skip QA), EMERGENCY (minimal output)
   - `fallback_strategies` per component:
     - `java_parser`: javalang → regex fallback → error
     - `llm_translator`: CodeT5+ → DeepSeek-Coder → pattern-matching → error  
     - `asset_converter`: AI → rule-based → skip
     - `qa_validator`: full → reduced → skip
   - `degradation_triggers`: timeout, api_error, parse_error, validation_error
   - `partial_output_minimum`: 0.3 (30% minimum for partial success)

2. Update `conversion_crew.py`:
   - Import and initialize DegradationManager and PartialConverter
   - Wrap agent calls in `execute_with_fallback()` 
   - Track component success/failure with PartialConverter
   - On degradation: log warning, reduce validation, continue
   - On completion: check if partial output available, generate report
   - Return DegradedConversionResult with completeness info
  </action>
  <verify>python -c "import yaml; from ai_engine.config import load_config; c = load_config('degradation_config.yaml'); print('Config loads OK')" && grep -l "DegradationManager" ai-engine/crew/conversion_crew.py</verify>
  <done>Config file created and conversion_crew.py integrates degradation handling</done>
</task>

</tasks>

<verification>
- [ ] PartialConverter can track successes/failures and generate partial output
- [ ] DegradationManager executes fallback chains correctly
- [ ] Config file defines all degradation levels and fallback strategies
- [ ] conversion_crew.py uses degradation system on failures
- [ ] Tests pass: degradation scenarios produce partial results
</verification>

<success_criteria>

- All tasks completed
- Partial conversions produce usable .mcaddon output when sub-components fail
- Fallback strategies trigger appropriately on errors/timeouts
- Degraded mode reduces validation but continues conversion
- Users receive completeness percentage and warnings in results
- Integration with Phase 10-01 timeout system works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/10-timeout-robustness/10-02-graceful-degradation/10-02-SUMMARY.md`
</output>
