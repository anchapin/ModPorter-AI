# Phase 10-04: Output Integrity Checks

## Phase Overview

**Phase Number**: 10-04  
**Phase Name**: Output Integrity Checks  
**Milestone**: v4.1 - Conversion Robustness

## Goal

Implement comprehensive validation and integrity checks for generated Bedrock output files, ensuring all conversions meet quality standards before delivery to users.

## Requirements Coverage

- Output Validation & Integrity: Deep validation of Bedrock output, file integrity
- Package integrity verification
- Output completeness verification
- Quality gate enforcement

## Success Criteria

- All generated packages pass integrity validation
- Completeness percentage accurately reported
- Quality gates enforced before release
- No corrupted files delivered to users
- Clear validation reports generated

---

## Plan: Output Integrity Validation System

### Wave 1: Package Integrity Foundation

#### Task 1.1: Create OutputIntegrityValidator

**Verify**: Unit tests pass for package validation

```python
# ai-engine/validators/output_integrity_validator.py
class OutputIntegrityValidator:
    """Validates output package integrity and completeness."""
    
    def __init__(self, config: OutputIntegrityConfig):
        self.config = config
        self.manifest_validator = ManifestValidator()
        self.file_integrity_checker = FileIntegrityChecker()
        
    async def validate_package(self, package_path: str) -> ValidationResult:
        """Validate entire .mcaddon package."""
        # 1. Check package is valid zip
        # 2. Validate manifest.json
        # 3. Verify required files present
        # 4. Check file integrity
```

**Dependencies**: None  
**Risk**: LOW - Pure validation logic

---

#### Task 1.2: Implement Manifest Validation

**Verify**: Unit tests pass for manifest validation

```python
# ai-engine/validators/manifest_validator.py
class ManifestValidator:
    """Validates Bedrock manifest.json files."""
    
    REQUIRED_FIELDS = ['header', 'modules', 'dependencies']
    
    def validate(self, manifest: dict) -> ManifestValidationResult:
        """Validate manifest structure and content."""
        # 1. Check required fields present
        # 2. Validate header (name, version, uuid)
        # 3. Validate modules
        # 4. Validate dependencies
```

**Dependencies**: Task 1.1  
**Risk**: LOW - Validation logic

---

#### Task 1.3: Implement File Integrity Checker

**Verify**: Unit tests pass for file integrity checks

```python
# ai-engine/validators/file_integrity_checker.py
class FileIntegrityChecker:
    """Checks file integrity within packages."""
    
    SUPPORTED_JSON_EXTENSIONS = ['.json', '.jsonc']
    SUPPORTED_JS_EXTENSIONS = ['.js', '.ts']
    
    def check_integrity(self, package_path: str) -> IntegrityResult:
        """Check all files in package for integrity."""
        # 1. Extract package
        # 2. Validate JSON syntax
        # 3. Check file paths (no traversal)
        # 4. Generate checksums
```

**Dependencies**: Task 1.1  
**Risk**: LOW - Validation logic

---

### Wave 2: Completeness & Correlation

#### Task 2.1: Create CompletenessTracker

**Verify**: Unit tests verify completeness tracking

```python
# ai-engine/validators/completeness_tracker.py
class CompletenessTracker:
    """Tracks output completeness vs. expected components."""
    
    async def verify_completeness(
        self, 
        input_analysis: ModAnalysis,
        output_package: str
    ) -> CompletenessResult:
        """Verify all expected components were generated."""
        # 1. Get expected components from input analysis
        # 2. List generated files
        # 3. Match components
        # 4. Calculate completeness percentage
```

**Dependencies**: Task 1.1, 1.2, 1.3  
**Risk**: MEDIUM - Needs integration with existing analysis

---

#### Task 2.2: Implement Output-to-Input Correlation

**Verify**: Unit tests verify correlation detection

```python
# ai-engine/validators/correlation_checker.py
class CorrelationChecker:
    """Verifies output components match input mod."""
    
    async def verify_correlation(
        self,
        input_mod: ModAnalysis,
        output_package: str
    ) -> CorrelationResult:
        """Verify output matches input structure."""
        # 1. Map input elements to output files
        # 2. Detect orphaned output
        # 3. Flag missing components
        # 4. Generate mapping report
```

**Dependencies**: Task 2.1  
**Risk**: MEDIUM - Requires understanding of mod structure

---

### Wave 3: Deep Validation & Quality Gates

#### Task 3.1: Implement Bedrock JSON Schema Validation

**Verify**: Unit tests pass for schema validation

```python
# ai-engine/validators/bedrock_schema_validator.py
class BedrockSchemaValidator:
    """Deep validation of Bedrock JSON files."""
    
    SCHEMAS = {
        'blocks': 'blocks.json',
        'items': 'items.json',
        'recipes': 'recipes.json',
        'loot_tables': 'loot_tables/*.json',
        'recipes_custom': 'recipes/*.json',
    }
    
    def validate_all(self, package_path: str) -> SchemaValidationResult:
        """Validate all Bedrock JSON files against schemas."""
        # 1. Load JSON schemas
        # 2. Validate each file type
        # 3. Check semantic correctness
```

**Dependencies**: Task 1.3  
**Risk**: LOW - Validation logic

---

#### Task 3.2: Implement Quality Gate System

**Verify**: Unit tests verify quality thresholds

```python
# ai-engine/validators/quality_gate.py
class QualityGate:
    """Enforces quality thresholds before release."""
    
    DEFAULT_THRESHOLDS = {
        'completeness': 0.80,  # 80% minimum
        'syntax_valid': 1.0,    # 100% JSON/JS must be valid
        'integrity': 1.0,       # 100% integrity required
    }
    
    def evaluate(self, validation_result: ValidationResult) -> QualityGateResult:
        """Evaluate if output passes quality gates."""
        # 1. Check completeness threshold
        # 2. Check syntax validity
        # 3. Check integrity
        # 4. Return pass/fail with details
```

**Dependencies**: Task 2.1, 3.1  
**Risk**: LOW - Logic configuration

---

#### Task 3.3: Implement Integrity Hasher

**Verify**: Unit tests verify hash generation

```python
# ai-engine/validators/integrity_hasher.py
class IntegrityHasher:
    """Generates checksums for output files."""
    
    def generate_hashes(self, package_path: str) -> HashResult:
        """Generate SHA256 hashes for all files."""
        # 1. Walk package contents
        # 2. Generate hash per file
        # 3. Generate package hash
        # 4. Return hash manifest
```

**Dependencies**: Task 1.3  
**Risk**: LOW - Hash computation

---

### Wave 4: Integration & Reporting

#### Task 4.1: Integrate with Conversion Pipeline

**Verify**: End-to-end test passes

```python
# ai-engine/validators/integrity_pipeline.py
class IntegrityValidationPipeline:
    """Full pipeline for output integrity validation."""
    
    async def validate_conversion_output(
        self,
        conversion_id: str,
        input_analysis: ModAnalysis,
        output_package: str
    ) -> IntegrityValidationReport:
        """Run full integrity validation."""
        # 1. Package integrity check
        # 2. Completeness verification
        # 3. Correlation check
        # 4. Schema validation
        # 5. Quality gate evaluation
        # 6. Generate hashes
```

**Dependencies**: Tasks 1.1, 2.1, 2.2, 3.1, 3.2, 3.3  
**Risk**: MEDIUM - Pipeline integration

---

#### Task 4.2: Create Integrity Report Generator

**Verify**: Unit tests generate correct report formats

```python
# ai-engine/validators/integrity_report.py
class IntegrityReportGenerator:
    """Generates detailed integrity validation reports."""
    
    FORMATS = ['json', 'html', 'markdown']
    
    def generate_report(
        self,
        validation_result: IntegrityValidationReport,
        format: str = 'json'
    ) -> str:
        """Generate report in specified format."""
        # 1. Format validation results
        # 2. Include completeness matrix
        # 3. List any failures
        # 4. Provide recommendations
```

**Dependencies**: Task 4.1  
**Risk**: LOW - Report generation

---

#### Task 4.3: Add API Endpoint for Integrity Checks

**Verify**: API tests pass

```python
# backend/src/api/integrity.py
@router.post("/api/v1/conversions/{conversion_id}/validate-integrity")
async def validate_conversion_integrity(
    conversion_id: str,
    db: AsyncSession
) -> IntegrityValidationReport:
    """Validate conversion output integrity."""
    # 1. Get conversion record
    # 2. Load input analysis
    # 3. Run integrity pipeline
    # 4. Return validation report
```

**Dependencies**: Task 4.1, 4.2  
**Risk**: MEDIUM - Backend API integration

---

## Summary

| Wave | Tasks | Description |
|------|-------|-------------|
| 1 | 1.1-1.3 | Package integrity foundation |
| 2 | 2.1-2.2 | Completeness & correlation |
| 3 | 3.1-3.3 | Deep validation & quality gates |
| 4 | 4.1-4.3 | Integration & reporting |

**Total Tasks**: 10

**Dependencies**:
- Phase 10-01 (timeout management) - context
- Phase 10-02 (graceful degradation) - partial tracking
- Phase 10-03 (input validation) - input baseline
- v4.0 QA validators - extend

**Risk Profile**: LOW-MEDIUM
**Estimated Duration**: 2-3 days
