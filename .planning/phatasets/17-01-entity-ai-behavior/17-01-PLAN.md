---
phase: "17-01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-engine/agents/entity_converter.py
  - ai-engine/knowledge/patterns/entity_behavior_patterns.py
  - ai-engine/tests/test_entity_ai_conversion.py
autonomous: true
requirements:
  - CONV-17-01
---

<objective>
Implement comprehensive entity AI behavior conversion from Java mod goals/tasks to Bedrock behavior pack components, enabling conversion of complex mob AI logic.
</objective>

<context>
@ai-engine/agents/entity_converter.py (existing entity converter with basic behavior mappings)
@ai-engine/knowledge/patterns/java_patterns.py (existing Java patterns)
@ai-engine/templates/bedrock/ (existing Bedrock templates)

# Existing EntityConverter Structure
The current EntityConverter has:
- MobCategory/EntityType enums
- EntityProperties dataclass
- Basic behavior_mappings dict (12 entries)
- convert_entities() method

# Bedrock Behavior Pack Components
Bedrock entities use behavior components like:
- minecraft:behavior.* (40+ behaviors)
- minecraft:goal.* (for targeting)
- minecraft:component_groups for state management
- minecraft:triggers for events
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Behavior Mappings</name>
  <files>ai-engine/agents/entity_converter.py</files>
  <read_first>
    - ai-engine/agents/entity_converter.py (lines 1-120 for existing mappings)
  </read_first>
  <action>
Extend behavior_mappings dict with comprehensive Java to Bedrock mappings:

1. **Movement Behaviors**:
   - jump_attack → minecraft:behavior.jump_at_target
   - door_interact → minecraft:behavior.interact
   - water_sensor → minecraft:behavior.swim_in_water
   - leash → minecraft:behavior.leashable
   - move_through_village → minecraft:behavior.move_through_village

2. **Combat Behaviors**:
   - melee_attack → minecraft:behavior.melee_attack
   - ranged_attack → minecraft:behavior.ranged_attack
   - hurt_by_target → minecraft:behavior.hurt_on_hurt
   - owner_hurt_by_target → minecraft:behavior.defend_owner
   - nearest_attackable → minecraft:behavior.nearest_attackable_target
   - random_target → minecraft:behavior.random_stroll

3. **AI Control Behaviors**:
   - cat_jump_wall → minecraft:behavior.celestial_track
   - ocelot_sneak → minecraft:behavior.sneak
   - spider_terrain_sensor → minecraft:behavior.terrain_glide

4. **Add Goal class**:
   - priority: int
   - goal_key: str
   - target_key: Optional[str]
   - settings: dict

5. **Add convert_goal() method**:
   - Parse Java GoalKey to Bedrock behavior
   - Map priority to weight
   - Convert target selectors
</action>
<verify>
<automated>python -c "
from agents.entity_converter import EntityConverter
ec = EntityConverter()
# Check extended mappings exist
assert 'nearest_attackable' in ec.behavior_mappings
assert 'defend_owner' in ec.behavior_mappings
assert 'move_through_village' in ec.behavior_mappings
print('Behavior mappings extended successfully')
"</automated>
</verify>
<done>45+ behavior mappings covering movement, combat, and AI control</done>
</task>

<task type="auto">
  <name>Task 2: Create EntityBehaviorPatterns Module</name>
  <files>ai-engine/knowledge/patterns/entity_behavior_patterns.py</files>
  <read_first>
    - ai-engine/knowledge/patterns/base.py (pattern base class)
  </read_first>
  <action>
Create entity behavior pattern library for RAG-based conversion:

1. **EntityBehaviorPattern dataclass**:
   - java_code_example: str
   - bedrock_behavior_component: str
   - conversion_notes: str
   - category: str (movement/combat/interaction/spawn)

2. **EntityBehaviorPatternLibrary class**:
   - patterns: List[EntityBehaviorPattern]
   - add_pattern(pattern)
   - search_by_java(java_code) → List[EntityBehaviorPattern]
   - get_all_by_category(category) → List[EntityBehaviorPattern]

3. **Add 30+ patterns** covering:
   - **Movement**: wander, flee, follow, patrol, migrate, swim, fly
   - **Combat**: attack, defend, revenge, avoid, intercept
   - **Interaction**: trade, ride, lead, breed, tempt
   - **Spawn**: spawn_egg, natural_spawn, spawner, egg

4. **Integration**:
   - Export via knowledge/patterns/__init__.py
</action>
<verify>
<automated>python -c "
from knowledge.patterns.entity_behavior_patterns import EntityBehaviorPatternLibrary
lib = EntityBehaviorPatternLibrary()
patterns = lib.search_by_java('GoalSelector')
assert len(patterns) > 0
print(f'Found {len(patterns)} patterns for GoalSelector')
"</automated>
</verify>
<done>30+ entity behavior patterns with RAG search capability</done>
</task>

<task type="auto">
  <name>Task 3: Implement Goal/Task Conversion</name>
  <files>ai-engine/agents/entity_converter.py</files>
  <read_first>
    - ai-engine/agents/entity_converter.py (existing EntityConverter)
  </read_first>
  <action>
Add goal/task conversion methods to EntityConverter:

1. **Add Goal dataclass**:
   ```python
   @dataclass
   class Goal:
       priority: int
       goal_key: str  # e.g., "core", "movement", "target"
       target_key: Optional[str]
       settings: Dict[str, Any]
   ```

2. **Add convert_goal() method**:
   - Convert Java Goal to Bedrock behavior component
   - Map goal priority (lower = higher priority in Java) to weight
   - Extract target selectors from @TargetOf annotations

3. **Add convert_goals() method**:
   - Parse List<Goal> from Java entity
   - Sort by priority
   - Convert to component group structure

4. **Add convert_behavior_group() method**:
   - Group behaviors by category
   - Create minecraft:component_groups
   - Add event triggers for state changes

5. **Add convert_event() method**:
   - Convert Java event handlers to Bedrock events
   - Map EntityJoinWorldEvent → on_spawn
   - Map EntityDeathEvent → on_death
   - Map ItemTossedEvent → on_item_used
</action>
<verify>
<automated>python -c "
from agents.entity_converter import EntityConverter
ec = EntityConverter()
# Test goal conversion
goal = ec.Goal(priority=1, goal_key='melee_attack', target_key='nearest', settings={})
result = ec.convert_goal(goal)
assert 'minecraft:behavior.melee_attack' in str(result).lower() or 'melee_attack' in str(result).lower()
print('Goal conversion works')
"</automated>
</verify>
<done>Complete goal/task conversion from Java to Bedrock</done>
</task>

<task type="auto">
  <name>Task 4: Create Unit Tests</name>
  <files>ai-engine/tests/test_entity_ai_conversion.py</files>
  <read_first>
    - ai-engine/tests/test_bedrock_builder_mvp.py (test pattern)
  </read_first>
  <action>
Create comprehensive unit tests for entity AI conversion:

1. **TestBehaviorMappings** (8 tests):
   - test_extended_mappings_complete
   - test_movement_behavior_mapping
   - test_combat_behavior_mapping
   - test_ai_control_behavior_mapping

2. **TestGoalConversion** (6 tests):
   - test_single_goal_conversion
   - test_goal_priority_mapping
   - test_target_selector_conversion
   - test_multiple_goals_sorted

3. **TestEntityBehaviorPatterns** (6 tests):
   - test_pattern_library_search
   - test_category_filtering
   - test_pattern_ranking

4. **TestEventConversion** (5 tests):
   - test_spawn_event_conversion
   - test_death_event_conversion
   - test_custom_event_conversion

5. **TestIntegration** (3 tests):
   - test_full_entity_with_ai
   - test_hostile_mob_conversion
   - test_passive_mob_conversion
</action>
<verify>
<automated>python -m pytest ai-engine/tests/test_entity_ai_conversion.py -v --tb=short 2>&1 | tail -30</automated>
</verify>
<done>28 unit tests covering all entity AI conversion functionality</done>
</task>

</tasks>

<verification>
Run import test:
<automated>python -c "from agents.entity_converter import EntityConverter; from knowledge.patterns.entity_behavior_patterns import EntityBehaviorPatternLibrary; print('All imports OK')"</automated>
</verification>

<success_criteria>
- [ ] 45+ behavior mappings in EntityConverter
- [ ] EntityBehaviorPatternLibrary with 30+ patterns
- [ ] Goal/task conversion working
- [ ] Event conversion for spawn/death
- [ ] 28 unit tests passing
- [ ] All imports work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/17-01-entity-ai-behavior/17-01-SUMMARY.md`
</output>