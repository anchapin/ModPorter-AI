[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"

[tool.ruff]
exclude = [
    "tests/",
    "src/tests/",
]

[tool.ruff.lint]
ignore = [
    "E402",  # module level import not at top of file (ignore for test files)
]