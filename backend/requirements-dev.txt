# Development dependencies for backend
# Note: Base requirements including pytest are already in requirements.txt
# This file contains additional dev-only tools

# Additional testing tools (beyond what's in requirements.txt)
pytest-mock>=3.11.0

# Code Quality (if not already in requirements.txt)
# ruff and black are already in requirements.txt

# Development Tools
pre-commit>=3.0.0