# Core FastAPI for AI Engine API - align with backend version
fastapi==0.135.3
uvicorn[standard]==0.44.0
python-multipart==0.0.26

# AI Framework - add upper bounds to avoid dependency conflicts
# Note: crewai requires Python 3.10-3.13. Use Docker container for development (Python 3.12).
# If running locally, use Python 3.12 virtual environment.
crewai>=0.11.0,<1.0.0
langchain>=0.3.0,<1.0.0
langchain-openai>=0.2.0,<1.0.0
langchain-ollama>=0.1.0,<1.0.0
openai>=1.0.0,<3.0.0
sentence-transformers>=2.2.0,<6.0.0

# Vector Database (pin version for embedchain compatibility)
# Note: chromadb may require Python <3.13
chromadb==1.1.1

# Data Processing - align with backend version
numpy
python-dotenv~=1.2.1

# Java analysis - align with backend version
javalang==0.13.0

# File processing - align with backend version
python-magic==0.4.27
Pillow>=10.0.0
pydub
jsonschema

# HTTP Client - align with backend version
httpx==0.28.1
beautifulsoup4>=4.12.0 # For web scraping (BedrockDocsScraper)
aiofiles>=23.0.0     # For async file operations (potentially in scraper or other utils)
validators>=0.22.0   # For URL validation (BedrockDocsScraper)

# Web Search - use available version
duckduckgo-search>=5.3.0

# Redis for job management - align with backend version
redis>=7.1.0

# Configuration Management - align with backend version
pydantic~=2.12.5
pydantic-settings~=2.11.0

# Monitoring - align with backend version
prometheus_client>=0.17.0
structlog>=24.0.0

# Distributed Tracing - OpenTelemetry
# Note: chromadb provides opentelemetry-exporter-otlp-proto-grpc which conflicts
# with opentelemetry-exporter-jaeger-proto-grpc on protobuf versions.
# Using flexible versions and letting pip/legacy-resolver find a solution.
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp>=1.24.0
opentelemetry-instrumentation-fastapi>=0.45b0
opentelemetry-instrumentation-httpx>=0.45b0
opentelemetry-instrumentation-redis>=0.45b0
